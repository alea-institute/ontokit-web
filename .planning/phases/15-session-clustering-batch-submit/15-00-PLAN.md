---
phase: 15-session-clustering-batch-submit
plan: 0
type: execute
wave: 0
depends_on: []
files_modified:
  - __tests__/lib/stores/shardPreviewStore.test.ts
  - __tests__/lib/api/clusterApi.test.ts
  - __tests__/lib/hooks/useShardDragDrop.test.ts
autonomous: true
requirements:
  - CLUSTER-01
  - CLUSTER-02
  - CLUSTER-03
  - CLUSTER-04
  - CLUSTER-05
  - CLUSTER-06
  - CLUSTER-07
  - CLUSTER-08

must_haves:
  truths:
    - "Test stubs exist for shardPreviewStore covering CLUSTER-01 through CLUSTER-05 and CLUSTER-08"
    - "Test stubs exist for cluster/batchSubmit API client covering CLUSTER-07"
    - "Test stubs exist for shard drag-drop hook covering CLUSTER-06"
    - "npm run test -- --run passes with all stubs skipped"
  artifacts:
    - path: "__tests__/lib/stores/shardPreviewStore.test.ts"
      provides: "Store unit test stubs"
      contains: "@pytest.mark.skip"
    - path: "__tests__/lib/api/clusterApi.test.ts"
      provides: "API client test stubs"
      contains: "describe"
    - path: "__tests__/lib/hooks/useShardDragDrop.test.ts"
      provides: "DnD hook test stubs"
      contains: "describe"
  key_links: []
---

<objective>
Create test stubs (Nyquist compliance) for all Phase 15 testable units before implementation begins.

Purpose: Ensure `npm run test -- --run` passes before any implementation, and downstream plans can verify their implementations against real test files without discovery failures.
Output: Three test stub files with skipped test cases covering CLUSTER-01 through CLUSTER-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-session-clustering-batch-submit/15-RESEARCH.md
@.planning/phases/15-session-clustering-batch-submit/15-VALIDATION.md

@__tests__/lib/stores/suggestionStore.test.ts (test pattern reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 test stubs for shardPreviewStore, cluster API, and DnD hook</name>
  <files>
    __tests__/lib/stores/shardPreviewStore.test.ts,
    __tests__/lib/api/clusterApi.test.ts,
    __tests__/lib/hooks/useShardDragDrop.test.ts
  </files>
  <read_first>
    __tests__/lib/stores/suggestionStore.test.ts,
    __tests__/lib/api/generation.test.ts,
    __tests__/lib/hooks/useSuggestions.test.ts
  </read_first>
  <action>
Create three test stub files following the established Phase 11-14 pattern: `@pytest.mark.skip` decorator equivalent in Vitest (use `it.skip(...)` for each test case).

**File 1: `__tests__/lib/stores/shardPreviewStore.test.ts`**

```typescript
import { describe, it, expect } from "vitest";

describe("shardPreviewStore", () => {
  // CLUSTER-01: setFromClusterResponse populates shard plan
  it.skip("setFromClusterResponse populates prGroups and shards from cluster API response", () => {
    // Downstream: 15-01-PLAN task 1
  });

  // CLUSTER-02: max 50 entities per shard
  it.skip("no shard has more than 50 entityIris after setFromClusterResponse", () => {
    // Downstream: 15-01-PLAN task 1
  });

  // CLUSTER-03: min 3 entities per non-misc shard
  it.skip("no non-misc shard has fewer than 3 entityIris after setFromClusterResponse", () => {
    // Downstream: 15-01-PLAN task 1
  });

  // CLUSTER-04: cross-cutting shard
  it.skip("cross-cutting entities appear in a dedicated shard with isCrossCutting=true", () => {
    // Downstream: 15-01-PLAN task 1
  });

  // CLUSTER-05: moveEntity atomicity
  it.skip("moveEntity removes entity from source shard and adds to target shard atomically", () => {
    // Downstream: 15-01-PLAN task 1
  });

  it.skip("moveEntity ensures entity appears in exactly one shard post-move", () => {
    // Downstream: 15-01-PLAN task 1
  });

  // CLUSTER-05: mergeShards
  it.skip("mergeShards combines entityIris and removes source shard", () => {
    // Downstream: 15-01-PLAN task 1
  });

  // CLUSTER-05: splitShard
  it.skip("splitShard creates new shard with selected entities and removes them from original", () => {
    // Downstream: 15-01-PLAN task 1
  });

  // CLUSTER-08: large session produces multiple PR groups
  it.skip("cluster response with >10 shards produces multiple PR groups", () => {
    // Downstream: 15-01-PLAN task 1
  });

  // moveShard between PR groups
  it.skip("moveShard moves shard from one PR group to another", () => {
    // Downstream: 15-01-PLAN task 1
  });

  // clear resets all state
  it.skip("clear resets prGroups, shards, and prGroupOrder to empty", () => {
    // Downstream: 15-01-PLAN task 1
  });
});
```

**File 2: `__tests__/lib/api/clusterApi.test.ts`**

```typescript
import { describe, it, expect } from "vitest";

describe("suggestionsApi.cluster", () => {
  // CLUSTER-07: cluster API call
  it.skip("cluster() sends POST with session_id and suggestion_items", () => {
    // Downstream: 15-01-PLAN task 2
  });

  it.skip("cluster() returns ClusterResponse with pr_groups, total counts, skip_clustering flag", () => {
    // Downstream: 15-01-PLAN task 2
  });
});

describe("suggestionsApi.batchSubmit", () => {
  // CLUSTER-07: batch submit API call
  it.skip("batchSubmit() sends POST with session_id, pr_groups, and optional notes", () => {
    // Downstream: 15-01-PLAN task 2
  });

  it.skip("batchSubmit() returns BatchSubmitResponse with per-PR results and success/failure counts", () => {
    // Downstream: 15-01-PLAN task 2
  });
});
```

**File 3: `__tests__/lib/hooks/useShardDragDrop.test.ts`**

```typescript
import { describe, it, expect } from "vitest";

describe("useShardDragDrop", () => {
  // CLUSTER-06: drag entity between shards
  it.skip("handleDragEnd with entity type calls moveEntity on store", () => {
    // Downstream: 15-03-PLAN task 2
  });

  // CLUSTER-06: drag shard between PR groups
  it.skip("handleDragEnd with shard type calls moveShard on store", () => {
    // Downstream: 15-03-PLAN task 2
  });

  // Invalid drop target
  it.skip("handleDragEnd with no over target does nothing", () => {
    // Downstream: 15-03-PLAN task 2
  });
});
```

All test files use `it.skip(...)` so they are discovered but do not fail.
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/ontokit-web" && npm run test -- --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - __tests__/lib/stores/shardPreviewStore.test.ts exists and contains "describe(\"shardPreviewStore\""
    - __tests__/lib/stores/shardPreviewStore.test.ts contains at least 11 it.skip calls
    - __tests__/lib/api/clusterApi.test.ts exists and contains "describe(\"suggestionsApi.cluster\""
    - __tests__/lib/api/clusterApi.test.ts contains at least 4 it.skip calls
    - __tests__/lib/hooks/useShardDragDrop.test.ts exists and contains "describe(\"useShardDragDrop\""
    - __tests__/lib/hooks/useShardDragDrop.test.ts contains at least 3 it.skip calls
    - npm run test -- --run exits with code 0
  </acceptance_criteria>
  <done>All three test stub files exist with skipped test cases. npm run test -- --run passes (all stubs skipped, no failures).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries introduced — test stubs only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| — | — | — | — | No threats — test stubs only |
</threat_model>

<verification>
`npm run test -- --run` passes with 0 failures. All three test files are discovered by Vitest.
</verification>

<success_criteria>
- Three test stub files created matching VALIDATION.md requirements
- All stubs use `it.skip()` pattern consistent with Phase 11-14
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/15-session-clustering-batch-submit/15-00-SUMMARY.md`
</output>
