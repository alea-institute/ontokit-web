---
phase: 08-optional-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/damienriehl/Coding Projects/ontokit-api/ontokit/core/config.py
  - /home/damienriehl/Coding Projects/ontokit-api/ontokit/core/auth.py
  - /home/damienriehl/Coding Projects/ontokit-api/tests/unit/test_auth_disabled.py
autonomous: true
requirements: [AUTH-01, AUTH-04, AUTH-05]

must_haves:
  truths:
    - "API starts without Zitadel configuration when AUTH_DISABLED=true"
    - "API returns data (not 401) on GET requests without Bearer tokens when AUTH_DISABLED=true"
    - "API returns an anonymous user with viewer role when AUTH_DISABLED=true"
    - "API works exactly as before when AUTH_DISABLED is unset or false"
  artifacts:
    - path: "/home/damienriehl/Coding Projects/ontokit-api/ontokit/core/config.py"
      provides: "AUTH_DISABLED setting"
      contains: "auth_disabled"
    - path: "/home/damienriehl/Coding Projects/ontokit-api/ontokit/core/auth.py"
      provides: "Anonymous user bypass when AUTH_DISABLED=true"
      contains: "ANONYMOUS_USER"
    - path: "/home/damienriehl/Coding Projects/ontokit-api/tests/unit/test_auth_disabled.py"
      provides: "Tests for auth disabled behavior"
      contains: "test_"
  key_links:
    - from: "ontokit/core/config.py"
      to: "ontokit/core/auth.py"
      via: "settings.auth_disabled"
      pattern: "settings\\.auth_disabled"
    - from: "ontokit/core/auth.py"
      to: "API routes"
      via: "RequiredUser/OptionalUser dependency injection"
      pattern: "ANONYMOUS_USER"
---

<objective>
Make the ontokit-api backend auth optional via AUTH_DISABLED env var.

Purpose: When AUTH_DISABLED=true, the API should serve all GET endpoints without requiring Bearer tokens, returning an anonymous viewer user. This enables public read-only browsing of ontology content. Write endpoints remain effectively protected because the anonymous user has only "viewer" role.

Output: Modified config.py with auth_disabled setting, modified auth.py with anonymous user bypass, and tests confirming both modes work.
</objective>

<execution_context>
@/home/damienriehl/.claude/get-shit-done/workflows/execute-plan.md
@/home/damienriehl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

Working directory for this plan: /home/damienriehl/Coding Projects/ontokit-api
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AUTH_DISABLED config setting and anonymous user bypass to auth module</name>
  <files>
    /home/damienriehl/Coding Projects/ontokit-api/ontokit/core/config.py
    /home/damienriehl/Coding Projects/ontokit-api/ontokit/core/auth.py
  </files>
  <read_first>
    /home/damienriehl/Coding Projects/ontokit-api/ontokit/core/config.py
    /home/damienriehl/Coding Projects/ontokit-api/ontokit/core/auth.py
  </read_first>
  <action>
1. In `ontokit/core/config.py`, add to the Settings class (after the `debug` field):
   ```python
   # Auth bypass for public read-only mode
   auth_disabled: bool = False
   ```

2. In `ontokit/core/auth.py`, add a module-level constant for the anonymous user (after the `ZITADEL_ROLES_CLAIM` line):
   ```python
   # Anonymous user returned when AUTH_DISABLED=true
   ANONYMOUS_USER = CurrentUser(
       id="anonymous",
       email=None,
       name="Anonymous",
       username="anonymous",
       roles=["viewer"],
   )
   ```
   NOTE: This must be placed AFTER the CurrentUser class definition (around line 68).

3. Modify `get_current_user()` (line ~247): Add an early return at the very top of the function body, BEFORE the credentials check:
   ```python
   if settings.auth_disabled:
       return ANONYMOUS_USER
   ```

4. Modify `get_current_user_optional()` (line ~288): Add an early return at the very top of the function body, BEFORE the credentials check:
   ```python
   if settings.auth_disabled:
       return ANONYMOUS_USER
   ```
   Rationale: Return the anonymous user (not None), so downstream code that checks `if user:` still works and pages render properly.

5. Modify `get_current_user_with_token()` (line ~305): Add an early return at the very top of the function body, BEFORE the credentials check:
   ```python
   if settings.auth_disabled:
       return ANONYMOUS_USER, "anonymous"
   ```

6. Do NOT modify the `get_jwks()`, `validate_token()`, or `fetch_userinfo()` functions. They simply won't be called when auth is disabled because the early returns bypass them.

7. Do NOT modify the `PermissionChecker` class or `require_roles()`. The anonymous user has only "viewer" role, so role-gated endpoints will naturally return 403 (which is correct — public users should not access admin endpoints).
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/ontokit-api" && python -c "
from ontokit.core.config import Settings
s = Settings(auth_disabled=True)
assert s.auth_disabled == True
s2 = Settings()
assert s2.auth_disabled == False
print('Config OK')

from ontokit.core.auth import ANONYMOUS_USER
assert ANONYMOUS_USER.id == 'anonymous'
assert ANONYMOUS_USER.roles == ['viewer']
assert ANONYMOUS_USER.name == 'Anonymous'
print('Anonymous user OK')
print('ALL CHECKS PASSED')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "auth_disabled: bool = False" /home/damienriehl/Coding\ Projects/ontokit-api/ontokit/core/config.py` returns 0
    - `grep -q "ANONYMOUS_USER" /home/damienriehl/Coding\ Projects/ontokit-api/ontokit/core/auth.py` returns 0
    - `grep -c "settings.auth_disabled" /home/damienriehl/Coding\ Projects/ontokit-api/ontokit/core/auth.py` returns 3 (one in each of the three functions)
    - No modifications to get_jwks, validate_token, fetch_userinfo, PermissionChecker, or require_roles
  </acceptance_criteria>
  <done>
    Settings class has auth_disabled field defaulting to False. All three auth dependency functions (get_current_user, get_current_user_optional, get_current_user_with_token) return ANONYMOUS_USER when auth_disabled=True. No other functions are modified. Existing behavior unchanged when auth_disabled is False (default).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tests for AUTH_DISABLED behavior</name>
  <files>
    /home/damienriehl/Coding Projects/ontokit-api/tests/unit/test_auth_disabled.py
  </files>
  <read_first>
    /home/damienriehl/Coding Projects/ontokit-api/ontokit/core/auth.py
    /home/damienriehl/Coding Projects/ontokit-api/ontokit/core/config.py
    /home/damienriehl/Coding Projects/ontokit-api/tests/conftest.py
    /home/damienriehl/Coding Projects/ontokit-api/tests/unit/test_projects_routes.py
  </read_first>
  <behavior>
    - Test 1: When auth_disabled=True, get_current_user returns ANONYMOUS_USER (id="anonymous", roles=["viewer"])
    - Test 2: When auth_disabled=True, get_current_user_optional returns ANONYMOUS_USER (not None)
    - Test 3: When auth_disabled=True, get_current_user_with_token returns (ANONYMOUS_USER, "anonymous")
    - Test 4: When auth_disabled=False (default), get_current_user raises 401 when no credentials provided
    - Test 5: When auth_disabled=False (default), get_current_user_optional returns None when no credentials provided
    - Test 6: ANONYMOUS_USER has correct properties (id, name, roles, is_superadmin=False)
  </behavior>
  <action>
Create `tests/unit/test_auth_disabled.py` with pytest tests using `unittest.mock.patch` to control `settings.auth_disabled`.

For each auth function test:
- Use `@pytest.mark.asyncio` since the auth functions are async
- Patch `ontokit.core.auth.settings` with a mock that has `auth_disabled=True` (or False)
- Call the function with `credentials=None`
- Assert the return value matches expected behavior

The tests should NOT require a running database or Zitadel instance. They test the auth module in isolation by patching settings.

Import pattern:
```python
import pytest
from unittest.mock import patch, MagicMock
from ontokit.core.auth import (
    get_current_user,
    get_current_user_optional,
    get_current_user_with_token,
    ANONYMOUS_USER,
    CurrentUser,
)
```

For patching, use: `@patch("ontokit.core.auth.settings")` and set `mock_settings.auth_disabled = True` on the mock.
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/ontokit-api" && python -m pytest tests/unit/test_auth_disabled.py -v --no-header 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `/home/damienriehl/Coding Projects/ontokit-api/tests/unit/test_auth_disabled.py`
    - `grep -c "def test_" /home/damienriehl/Coding\ Projects/ontokit-api/tests/unit/test_auth_disabled.py` returns at least 6
    - `grep -q "ANONYMOUS_USER" /home/damienriehl/Coding\ Projects/ontokit-api/tests/unit/test_auth_disabled.py` returns 0
    - `grep -q "auth_disabled" /home/damienriehl/Coding\ Projects/ontokit-api/tests/unit/test_auth_disabled.py` returns 0
    - All tests pass
  </acceptance_criteria>
  <done>
    Six or more tests cover both AUTH_DISABLED=true and AUTH_DISABLED=false (default) paths for all three auth dependency functions. All tests pass. Tests run in isolation without Zitadel or database.
  </done>
</task>

</tasks>

<verification>
1. Run the full test suite: `cd "/home/damienriehl/Coding Projects/ontokit-api" && python -m pytest tests/unit/test_auth_disabled.py -v`
2. Verify config default: `python -c "from ontokit.core.config import Settings; s = Settings(); assert s.auth_disabled == False"`
3. Grep verification: `grep -c "settings.auth_disabled" ontokit/core/auth.py` should return 3
</verification>

<success_criteria>
- AUTH_DISABLED=true causes all three auth functions to return anonymous user without contacting Zitadel
- AUTH_DISABLED unset or false preserves all existing auth behavior (401 without credentials, JWT validation, etc.)
- All new tests pass
- No existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/08-optional-auth/08-01-SUMMARY.md`
</output>
