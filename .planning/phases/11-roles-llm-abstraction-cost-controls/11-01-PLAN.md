---
phase: 11-roles-llm-abstraction-cost-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ../ontokit-api/ontokit/models/llm_config.py
  - ../ontokit-api/ontokit/schemas/llm.py
  - ../ontokit-api/ontokit/services/llm/__init__.py
  - ../ontokit-api/ontokit/services/llm/base.py
  - ../ontokit-api/ontokit/services/llm/registry.py
  - ../ontokit-api/ontokit/services/llm/openai_compat.py
  - ../ontokit-api/ontokit/services/llm/anthropic_provider.py
  - ../ontokit-api/ontokit/services/llm/google_provider.py
  - ../ontokit-api/ontokit/services/llm/cohere_provider.py
  - ../ontokit-api/ontokit/services/llm/github_models_provider.py
  - ../ontokit-api/ontokit/services/llm/pricing.py
  - ../ontokit-api/ontokit/services/llm/crypto.py
  - ../ontokit-api/ontokit/services/llm/ssrf.py
  - ../ontokit-api/alembic/versions/add_llm_config_and_audit_tables.py
  - ../ontokit-api/ontokit/models/project_member.py
autonomous: true
requirements: [LLM-01, LLM-02, LLM-05, LLM-06]

must_haves:
  truths:
    - "ProjectLLMConfig model can store provider, encrypted API key, model tier, budget, and base_url"
    - "LLMAuditLog model can record every LLM call with timestamp, user, model, tokens, cost, and BYO flag"
    - "ProjectMember model has can_self_merge_structural boolean field"
    - "get_provider() factory returns a provider instance for any of the 13 LLMProviderType values"
    - "API key encryption and decryption round-trips correctly using Fernet"
    - "SSRF validation blocks private IP ranges for custom/local base_url values"
  artifacts:
    - path: "../ontokit-api/ontokit/models/llm_config.py"
      provides: "ProjectLLMConfig and LLMAuditLog SQLAlchemy models"
      contains: "class ProjectLLMConfig"
    - path: "../ontokit-api/ontokit/services/llm/registry.py"
      provides: "get_provider() factory and LLMProviderType enum"
      exports: ["get_provider", "LLMProviderType"]
    - path: "../ontokit-api/ontokit/services/llm/crypto.py"
      provides: "Fernet encrypt/decrypt helpers"
      exports: ["encrypt_secret", "decrypt_secret"]
    - path: "../ontokit-api/ontokit/services/llm/pricing.py"
      provides: "LiteLLM pricing fetch with 7-day cache"
      exports: ["get_model_pricing"]
  key_links:
    - from: "../ontokit-api/ontokit/services/llm/registry.py"
      to: "../ontokit-api/ontokit/services/llm/openai_compat.py"
      via: "get_provider() dispatches to OpenAICompatProvider for 9 providers"
      pattern: "OpenAICompatProvider"
    - from: "../ontokit-api/ontokit/services/llm/crypto.py"
      to: "../ontokit-api/ontokit/services/embedding_service.py"
      via: "Same _get_fernet() key derivation pattern"
      pattern: "_get_fernet"
---

<objective>
Create the entire backend data layer and LLM provider registry for Phase 11.

Purpose: This is the foundation for all LLM features. DB models store per-project configuration and audit logs. The provider registry (ported from folio-enrich) enables dispatching calls to 13 different LLM providers. Crypto helpers encrypt API keys at rest. Pricing service fetches token costs from LiteLLM.

Output: SQLAlchemy models, Alembic migration, LLM service package with provider registry, crypto helpers, pricing service, and SSRF validator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-roles-llm-abstraction-cost-controls/11-CONTEXT.md
@.planning/phases/11-roles-llm-abstraction-cost-controls/11-RESEARCH.md

<interfaces>
<!-- Key types and contracts from existing codebase that executor needs. -->

From ontokit-api/ontokit/services/embedding_service.py (Fernet pattern to replicate):
```python
import base64, hashlib
from cryptography.fernet import Fernet

def _get_fernet() -> Fernet:
    from ontokit.core.config import settings
    key = hashlib.sha256(settings.secret_key.encode()).digest()
    return Fernet(base64.urlsafe_b64encode(key))

def _encrypt_secret(plaintext: str) -> str:
    return _get_fernet().encrypt(plaintext.encode()).decode()

def _decrypt_secret(ciphertext: str) -> str:
    return _get_fernet().decrypt(ciphertext.encode()).decode()
```

From ontokit-api/ontokit/models/embedding.py (DB model pattern to replicate):
```python
class ProjectEmbeddingConfig(Base):
    __tablename__ = "project_embedding_configs"
    id: Mapped[uuid.UUID] = mapped_column(primary_key=True, default=uuid.uuid4)
    project_id: Mapped[uuid.UUID] = mapped_column(ForeignKey("projects.id", ondelete="CASCADE"), unique=True)
    provider: Mapped[str] = mapped_column(String(50), default="local")
    api_key_encrypted: Mapped[str | None] = mapped_column(Text, nullable=True)
    ...
```

From folio-enrich LLMProviderType enum (13 providers — port verbatim):
openai, anthropic, google, mistral, cohere, meta_llama, ollama, lmstudio, custom, groq, xai, github_models, llamafile
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DB models, schemas, and Alembic migration</name>
  <files>
    ../ontokit-api/ontokit/models/llm_config.py
    ../ontokit-api/ontokit/schemas/llm.py
    ../ontokit-api/alembic/versions/add_llm_config_and_audit_tables.py
    ../ontokit-api/ontokit/models/project_member.py (modify — add can_self_merge_structural)
  </files>
  <read_first>
    ../ontokit-api/ontokit/models/embedding.py (template for ProjectLLMConfig model)
    ../ontokit-api/ontokit/models/project_member.py (existing model — add field)
    ../ontokit-api/ontokit/schemas/embedding.py (template for Pydantic schemas)
    ../ontokit-api/ontokit/core/database.py (Base import path)
    ../ontokit-api/alembic/versions/ (existing migration naming convention)
  </read_first>
  <action>
**Create `ontokit/models/llm_config.py`** with two SQLAlchemy models:

1. `ProjectLLMConfig`:
   - `id: Mapped[uuid.UUID]` primary key, default uuid4
   - `project_id: Mapped[uuid.UUID]` ForeignKey("projects.id", ondelete="CASCADE"), unique=True
   - `provider: Mapped[str]` String(50), default="openai"
   - `model: Mapped[str | None]` String(200), nullable=True
   - `model_tier: Mapped[str]` String(20), default="quality" (values: "quality" or "cheap")
   - `api_key_encrypted: Mapped[str | None]` Text, nullable=True
   - `base_url: Mapped[str | None]` String(500), nullable=True (for Ollama/local/custom)
   - `monthly_budget_usd: Mapped[float | None]` nullable=True (None = unlimited)
   - `daily_cap_usd: Mapped[float | None]` nullable=True (None = no daily cap)
   - `created_at: Mapped[datetime]` DateTime(timezone=True), server_default=func.now()
   - `updated_at: Mapped[datetime | None]` DateTime(timezone=True), onupdate=func.now()

2. `LLMAuditLog`:
   - `id: Mapped[uuid.UUID]` primary key, default uuid4
   - `project_id: Mapped[uuid.UUID]` ForeignKey("projects.id", ondelete="CASCADE")
   - `user_id: Mapped[str]` String(255)
   - `model: Mapped[str]` String(200)
   - `provider: Mapped[str]` String(50)
   - `endpoint: Mapped[str]` String(200) — e.g. "llm/generate-suggestions"
   - `input_tokens: Mapped[int]` Integer
   - `output_tokens: Mapped[int]` Integer
   - `cost_estimate_usd: Mapped[float]`
   - `is_byo_key: Mapped[bool]` Boolean, default=False
   - `created_at: Mapped[datetime]` DateTime(timezone=True), server_default=func.now()
   - `__table_args__`: Index("ix_llm_audit_project_date", "project_id", "created_at"), Index("ix_llm_audit_project_user", "project_id", "user_id")

**Create `ontokit/schemas/llm.py`** with Pydantic schemas:

1. `LLMProviderType` — str enum with 13 values: openai, anthropic, google, mistral, cohere, meta_llama, ollama, lmstudio, custom, groq, xai, github_models, llamafile
2. `LLMConfigResponse` — provider, model, model_tier, api_key_set (bool, NOT the key), base_url, monthly_budget_usd, daily_cap_usd
3. `LLMConfigUpdate` — provider, model, model_tier, api_key (optional, write-only), base_url, monthly_budget_usd, daily_cap_usd
4. `LLMStatusResponse` — configured (bool), provider (optional), budget_exhausted (bool), daily_remaining (int|None), monthly_budget_usd (float|None), monthly_spent_usd (float), burn_rate_daily_usd (float)
5. `LLMUsageResponse` — total_calls, total_cost_usd, budget_consumed_pct, burn_rate_daily_usd, users (list of LLMUserUsage)
6. `LLMUserUsage` — user_id, user_name, calls_today, calls_this_month, cost_this_month_usd, is_byo_key
7. `LLMAuditEntry` — timestamp, user_id, model, provider, endpoint, input_tokens, output_tokens, cost_estimate_usd, is_byo_key
8. `LLMProviderInfo` — provider (LLMProviderType), display_name (str), requires_api_key (bool), icon_name (str — lucide icon name)
9. `LLMKnownModel` — provider (LLMProviderType), model_id (str), display_name (str), tier (str — quality/cheap)

**Modify `ontokit/models/project_member.py`** — add `can_self_merge_structural: Mapped[bool] = mapped_column(Boolean, default=False)` to the existing ProjectMember model.

**Create Alembic migration** — `alembic revision --autogenerate -m "add_llm_config_audit_tables_and_member_flag"` then verify it creates:
   - `project_llm_configs` table
   - `llm_audit_logs` table with indexes
   - `can_self_merge_structural` column on `project_members`

Import the new models in `ontokit/models/__init__.py` if that file exists and uses explicit imports.
  </action>
  <verify>
    <automated>cd ../ontokit-api && uv run alembic upgrade head && uv run python -c "from ontokit.models.llm_config import ProjectLLMConfig, LLMAuditLog; from ontokit.schemas.llm import LLMProviderType, LLMConfigResponse; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `ontokit/models/llm_config.py` contains `class ProjectLLMConfig` with `api_key_encrypted`, `monthly_budget_usd`, `daily_cap_usd`, `model_tier`, `base_url`
    - `ontokit/models/llm_config.py` contains `class LLMAuditLog` with `is_byo_key`, `cost_estimate_usd`, `input_tokens`, `output_tokens`
    - `ontokit/schemas/llm.py` contains `class LLMProviderType` with all 13 enum values
    - `ontokit/schemas/llm.py` contains `class LLMConfigResponse` with `api_key_set: bool` (NOT `api_key`)
    - ProjectMember model file contains `can_self_merge_structural: Mapped[bool]`
    - Alembic migration file exists in `alembic/versions/` and references all three changes
    - `alembic upgrade head` succeeds without errors
  </acceptance_criteria>
  <done>All three DB model changes (ProjectLLMConfig, LLMAuditLog, can_self_merge_structural) exist with correct columns, schemas are defined, and Alembic migration applies cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Port LLM provider registry from folio-enrich and create crypto/pricing/SSRF services</name>
  <files>
    ../ontokit-api/ontokit/services/llm/__init__.py
    ../ontokit-api/ontokit/services/llm/base.py
    ../ontokit-api/ontokit/services/llm/registry.py
    ../ontokit-api/ontokit/services/llm/openai_compat.py
    ../ontokit-api/ontokit/services/llm/anthropic_provider.py
    ../ontokit-api/ontokit/services/llm/google_provider.py
    ../ontokit-api/ontokit/services/llm/cohere_provider.py
    ../ontokit-api/ontokit/services/llm/github_models_provider.py
    ../ontokit-api/ontokit/services/llm/pricing.py
    ../ontokit-api/ontokit/services/llm/crypto.py
    ../ontokit-api/ontokit/services/llm/ssrf.py
    ../ontokit-api/pyproject.toml (modify — add openai, anthropic, google-generativeai, cohere)
  </files>
  <read_first>
    ../folio-enrich/backend/app/services/llm/base.py (abstract LLMProvider class to port)
    ../folio-enrich/backend/app/services/llm/registry.py (get_provider factory to port)
    ../folio-enrich/backend/app/services/llm/openai_compat.py (OpenAICompatProvider to port)
    ../folio-enrich/backend/app/services/llm/anthropic_provider.py (to port)
    ../folio-enrich/backend/app/services/llm/google_provider.py (to port)
    ../folio-enrich/backend/app/services/llm/cohere_provider.py (to port)
    ../folio-enrich/backend/app/services/llm/github_models_provider.py (to port)
    ../folio-enrich/backend/app/services/llm/pricing.py (LiteLLM pricing fetch to port)
    ../folio-mapper/backend/app/services/llm/url_validator.py (SSRF validator to port)
    ../ontokit-api/ontokit/services/embedding_service.py (Fernet crypto pattern lines 41-52)
    ../ontokit-api/pyproject.toml (current dependencies)
  </read_first>
  <action>
**Install new Python dependencies** — add to `pyproject.toml` dependencies:
```
"openai>=1.0.0,<2.0.0"
"anthropic>=0.18.0,<1.0.0"
"google-generativeai>=0.8.0"
"cohere>=5.0.0"
```
Run `cd ../ontokit-api && uv add "openai>=1.0.0,<2.0.0" "anthropic>=0.18.0,<1.0.0" "google-generativeai>=0.8.0" "cohere>=5.0.0"`.

**Create `ontokit/services/llm/__init__.py`** — re-exports: `get_provider`, `LLMProvider`, `encrypt_secret`, `decrypt_secret`, `get_model_pricing`, `validate_base_url`.

**Create `ontokit/services/llm/base.py`** — Port the abstract `LLMProvider` base class from folio-enrich. Must include abstract methods:
- `async def chat(self, messages: list[dict], **kwargs) -> tuple[str, int, int]` (returns text, input_tokens, output_tokens)
- `async def test_connection(self) -> bool`
- `async def list_models(self) -> list[str]`

Constructor takes: `api_key: str | None`, `base_url: str | None`, `model: str`.

**Create `ontokit/services/llm/registry.py`** — Port `get_provider()` factory from folio-enrich:
- Import `LLMProviderType` from `ontokit.schemas.llm`
- Map provider types to classes with lazy imports (avoid SDK init cost for unused providers)
- `PROVIDER_DISPLAY_NAMES`: dict mapping each provider type to human-readable name
- `PROVIDER_REQUIRES_KEY`: dict mapping each provider type to bool (True for cloud, False for local)
- `PROVIDER_ICON_NAMES`: dict mapping each provider type to lucide icon name (Cpu for ollama/lmstudio/llamafile, Sparkles for openai, Bot for anthropic, Star for google, Zap for cohere, Globe for custom/github_models, etc.)
- `KNOWN_MODELS`: dict mapping provider type to list of `{"id": str, "name": str, "tier": "quality"|"cheap"}` entries. Include defaults from RESEARCH.md: openai gpt-4o (quality) / gpt-4o-mini (cheap), anthropic claude-sonnet-4-6 (quality) / claude-haiku-35-20241022 (cheap), google gemini-2.5-flash (quality) / gemini-2.0-flash-lite (cheap), etc.
- `get_provider(provider_type: str, api_key: str | None, base_url: str | None, model: str) -> LLMProvider`

**Create `ontokit/services/llm/openai_compat.py`** — Port `OpenAICompatProvider` from folio-enrich. This single class handles 9 providers (openai, mistral, meta_llama, ollama, lmstudio, custom, groq, xai, llamafile) by parameterizing the `base_url` in the OpenAI SDK. Must extract `usage.prompt_tokens` and `usage.completion_tokens` from OpenAI response.

**Create `ontokit/services/llm/anthropic_provider.py`** — Port from folio-enrich. Handle system message separately (Anthropic requires `system` param, not in messages array). Extract `input_tokens` and `output_tokens` from `response.usage`.

**Create `ontokit/services/llm/google_provider.py`** — Port from folio-enrich. Use `google.generativeai` SDK. Extract token counts from `response.usage_metadata` (prompt_token_count, candidates_token_count). Fallback: estimate from `len(text.split()) * 1.3`.

**Create `ontokit/services/llm/cohere_provider.py`** — Port from folio-enrich. Use `cohere` SDK. Extract token counts from response meta if available, fallback to estimation.

**Create `ontokit/services/llm/github_models_provider.py`** — Port from folio-enrich. Azure OpenAI endpoint variant.

**Create `ontokit/services/llm/crypto.py`** — Copy the Fernet pattern from `embedding_service.py` verbatim:
```python
import base64, hashlib
from cryptography.fernet import Fernet

def _get_fernet() -> Fernet:
    from ontokit.core.config import settings
    key = hashlib.sha256(settings.secret_key.encode()).digest()
    return Fernet(base64.urlsafe_b64encode(key))

def encrypt_secret(plaintext: str) -> str:
    return _get_fernet().encrypt(plaintext.encode()).decode()

def decrypt_secret(ciphertext: str) -> str:
    return _get_fernet().decrypt(ciphertext.encode()).decode()
```

**Create `ontokit/services/llm/pricing.py`** — Port from folio-enrich:
- `LITELLM_PRICING_URL = "https://raw.githubusercontent.com/BerriAI/litellm/main/model_prices_and_context_window.json"`
- `_pricing_cache: dict | None` and `_pricing_fetched_at: datetime | None` module globals
- `async def get_model_pricing(model: str) -> tuple[float, float]` — returns (input_cost_per_token, output_cost_per_token)
- Cache TTL: 7 days. On fetch failure, fall back to stale cache.
- Use `httpx.AsyncClient` for the fetch (already in ontokit-api deps).

**Create `ontokit/services/llm/ssrf.py`** — Port `url_validator.py` from folio-mapper:
- `validate_base_url(url: str, allow_private: bool = False) -> str` — returns validated URL
- Blocks: private IP ranges (10.x, 172.16-31.x, 192.168.x, 127.x, 169.254.x, ::1, fc00::, fe80::), file:// scheme, non-http(s) schemes
- Cloud providers: enforce https only
- Local providers (ollama, lmstudio, llamafile): allow http but still block metadata endpoints (169.254.169.254)
- Raises `ValueError` with descriptive message on failure

Per D-01, all providers use direct SDKs (not ALEA LLM Client or litellm as dispatch).
Per D-04, local model support is first-class (ollama/lmstudio/llamafile/custom all supported).
Per D-12, one active provider at a time per project.
  </action>
  <verify>
    <automated>cd ../ontokit-api && uv run python -c "
from ontokit.services.llm.registry import get_provider, KNOWN_MODELS, PROVIDER_DISPLAY_NAMES, LLMProviderType
from ontokit.services.llm.crypto import encrypt_secret, decrypt_secret
from ontokit.services.llm.ssrf import validate_base_url

# Test crypto round-trip
encrypted = encrypt_secret('test-key-123')
assert decrypt_secret(encrypted) == 'test-key-123', 'Crypto round-trip failed'

# Test provider registry has all 13 providers
assert len(PROVIDER_DISPLAY_NAMES) == 13, f'Expected 13 providers, got {len(PROVIDER_DISPLAY_NAMES)}'

# Test SSRF blocks private IPs
try:
    validate_base_url('http://169.254.169.254/')
    assert False, 'Should have blocked metadata endpoint'
except ValueError:
    pass

print('All checks passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - `ontokit/services/llm/registry.py` contains `def get_provider` and `PROVIDER_DISPLAY_NAMES` with 13 entries
    - `ontokit/services/llm/openai_compat.py` contains `class OpenAICompatProvider` with `async def chat`
    - `ontokit/services/llm/anthropic_provider.py` contains `class AnthropicProvider`
    - `ontokit/services/llm/google_provider.py` contains `class GoogleProvider`
    - `ontokit/services/llm/cohere_provider.py` contains `class CohereProvider`
    - `ontokit/services/llm/github_models_provider.py` contains `class GitHubModelsProvider`
    - `ontokit/services/llm/crypto.py` contains `def encrypt_secret` and `def decrypt_secret`
    - `ontokit/services/llm/ssrf.py` contains `def validate_base_url`
    - `ontokit/services/llm/pricing.py` contains `LITELLM_PRICING_URL` and `async def get_model_pricing`
    - `pyproject.toml` lists `openai`, `anthropic`, `google-generativeai`, `cohere` as dependencies
    - `uv run python -c "from ontokit.services.llm import get_provider, encrypt_secret, decrypt_secret"` succeeds
  </acceptance_criteria>
  <done>Complete LLM service package with 13-provider registry, 5 provider implementations, Fernet crypto, LiteLLM pricing, and SSRF validator — all importable and tested.</done>
</task>

</tasks>

<verification>
- `cd ../ontokit-api && uv run alembic upgrade head` succeeds
- `cd ../ontokit-api && uv run python -c "from ontokit.models.llm_config import ProjectLLMConfig, LLMAuditLog; print('models OK')"` succeeds
- `cd ../ontokit-api && uv run python -c "from ontokit.services.llm import get_provider, encrypt_secret, decrypt_secret; print('services OK')"` succeeds
- Crypto round-trip test passes
- SSRF validator blocks private IPs
</verification>

<success_criteria>
- All 13 LLM providers are registered in the provider registry
- ProjectLLMConfig and LLMAuditLog tables exist in the database
- ProjectMember has can_self_merge_structural column
- API key encryption/decryption round-trips correctly
- LiteLLM pricing fetch module exists (cache logic, URL constant)
- No new code stores BYO keys on the backend (per D-05)
</success_criteria>

<output>
After completion, create `.planning/phases/11-roles-llm-abstraction-cost-controls/11-01-SUMMARY.md`
</output>
