---
phase: 11-roles-llm-abstraction-cost-controls
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - ../ontokit-api/tests/unit/test_llm_config.py
  - ../ontokit-api/tests/unit/test_llm_rate_limit.py
  - ../ontokit-api/tests/unit/test_llm_budget.py
  - ../ontokit-api/tests/unit/test_llm_role_gates.py
  - ../ontokit-api/tests/unit/test_llm_audit.py
  - __tests__/lib/stores/byoKeyStore.test.ts
  - __tests__/lib/hooks/useLLMGate.test.ts
autonomous: true
requirements: [LLM-01, LLM-02, LLM-03, LLM-07, ROLE-01, ROLE-02, ROLE-03, ROLE-04, ROLE-05, COST-01, COST-02, COST-03, COST-04, COST-07]

must_haves:
  truths:
    - "All 7 test stub files exist with skip markers"
    - "Backend test stubs are discoverable by pytest"
    - "Frontend test stubs are discoverable by Vitest"
  artifacts:
    - path: "../ontokit-api/tests/unit/test_llm_config.py"
      provides: "Stubs for LLM config model tests (LLM-01, LLM-02, LLM-03)"
      contains: "pytest.mark.skip"
    - path: "../ontokit-api/tests/unit/test_llm_rate_limit.py"
      provides: "Stubs for rate limit tests (COST-03, COST-04)"
      contains: "pytest.mark.skip"
    - path: "../ontokit-api/tests/unit/test_llm_budget.py"
      provides: "Stubs for budget enforcement tests (COST-01, COST-02, COST-07)"
      contains: "pytest.mark.skip"
    - path: "../ontokit-api/tests/unit/test_llm_role_gates.py"
      provides: "Stubs for role gate tests (ROLE-01 through ROLE-05)"
      contains: "pytest.mark.skip"
    - path: "../ontokit-api/tests/unit/test_llm_audit.py"
      provides: "Stubs for audit logging tests (LLM-07)"
      contains: "pytest.mark.skip"
    - path: "__tests__/lib/stores/byoKeyStore.test.ts"
      provides: "Stub for BYO key store tests"
      contains: "it.skip"
    - path: "__tests__/lib/hooks/useLLMGate.test.ts"
      provides: "Stub for LLM gate hook tests"
      contains: "it.skip"
  key_links: []
---

<objective>
Create test stub files for all Phase 11 automated verification targets.

Purpose: Plans 01, 02, and 03 reference test files that do not yet exist. This Wave 0 plan creates skeleton test files with skip markers so that (a) the Nyquist rule is satisfied and (b) implementing plans can fill in test logic against real code. Without these stubs, `<verify>` commands in downstream plans would fail before implementation begins.

Output: 5 backend pytest stub files + 2 frontend Vitest stub files, all with skip markers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-roles-llm-abstraction-cost-controls/11-CONTEXT.md
@.planning/phases/11-roles-llm-abstraction-cost-controls/11-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend pytest test stubs with skip markers</name>
  <files>
    ../ontokit-api/tests/unit/test_llm_config.py
    ../ontokit-api/tests/unit/test_llm_rate_limit.py
    ../ontokit-api/tests/unit/test_llm_budget.py
    ../ontokit-api/tests/unit/test_llm_role_gates.py
    ../ontokit-api/tests/unit/test_llm_audit.py
  </files>
  <read_first>
    ../ontokit-api/tests/ (ls — discover existing test directory structure and naming conventions)
    ../ontokit-api/tests/unit/ (ls — check if unit directory exists, see existing test patterns)
  </read_first>
  <action>
Create 5 pytest test stub files in `../ontokit-api/tests/unit/`. If the `tests/unit/` directory does not exist, create it with an `__init__.py`.

Each file should contain:
- Module docstring explaining what it will test and which requirements it covers
- `import pytest` at the top
- Test functions with `@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan XX")` decorators
- Each function has a descriptive name matching the VALIDATION.md task IDs

**`test_llm_config.py`** (Plan 01):
```python
"""Tests for LLM config model — LLM-01, LLM-02, LLM-03."""
import pytest

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 01")
def test_config_round_trip():
    """ProjectLLMConfig stores and retrieves all fields correctly."""

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 01")
def test_api_key_encrypted():
    """API key is stored encrypted, decrypt returns original."""

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 01")
def test_byo_key_not_stored():
    """BYO key is never persisted to any DB column."""
```

**`test_llm_rate_limit.py`** (Plan 02):
```python
"""Tests for LLM rate limiting — COST-03, COST-04."""
import pytest

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 02")
def test_editor_rate_limit():
    """Editor is rate-limited to 500 calls/day per project."""

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 02")
def test_suggester_rate_limit():
    """Suggester is rate-limited to 100 calls/day per project."""
```

**`test_llm_budget.py`** (Plan 02):
```python
"""Tests for LLM budget enforcement — COST-01, COST-02, COST-07."""
import pytest

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 02")
def test_byo_excluded_from_budget():
    """BYO-key calls do not count against project budget."""

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 02")
def test_budget_exhaustion_402():
    """Budget exhaustion returns 402 and blocks further calls."""
```

**`test_llm_role_gates.py`** (Plan 02):
```python
"""Tests for LLM role-based access — ROLE-01 through ROLE-05."""
import pytest

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 02")
def test_anonymous_blocked():
    """Anonymous users get 403 on LLM endpoints."""

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 02")
def test_viewer_blocked():
    """Viewer role has no LLM access."""

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 02")
def test_admin_unlimited():
    """Admin/owner have unlimited rate limits."""

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 02")
def test_editor_access():
    """Editor has LLM access with 500/day limit."""

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 02")
def test_suggester_access():
    """Suggester has LLM access with 100/day limit."""
```

**`test_llm_audit.py`** (Plan 02):
```python
"""Tests for LLM audit logging — LLM-07."""
import pytest

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 02")
def test_audit_log_written():
    """Every LLM call creates an audit log entry with metadata."""

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 02")
def test_audit_no_content():
    """Audit log does not store prompt or response content."""
```

Ensure all files are valid Python that pytest can collect (even if all tests skip).
  </action>
  <verify>
    <automated>cd ../ontokit-api && uv run pytest tests/unit/test_llm_*.py --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <done>5 backend test stub files exist in tests/unit/, all discoverable by pytest, all tests marked with skip.</done>
</task>

<task type="auto">
  <name>Task 2: Create frontend Vitest test stubs with skip markers</name>
  <files>
    __tests__/lib/stores/byoKeyStore.test.ts
    __tests__/lib/hooks/useLLMGate.test.ts
  </files>
  <read_first>
    __tests__/ (ls — discover existing test directory structure)
    vitest.config.ts (test configuration and patterns)
  </read_first>
  <action>
Create 2 Vitest test stub files. Create directories if they do not exist (`__tests__/lib/stores/` and `__tests__/lib/hooks/`).

**`__tests__/lib/stores/byoKeyStore.test.ts`** (Plan 03):
```typescript
import { describe, it } from "vitest";

describe("byoKeyStore", () => {
  it.skip("stores BYO key by projectId in localStorage", () => {
    // Wave 0 stub — implementation in Plan 03
  });

  it.skip("retrieves stored BYO key for a project", () => {
    // Wave 0 stub — implementation in Plan 03
  });

  it.skip("clears BYO key for a project", () => {
    // Wave 0 stub — implementation in Plan 03
  });

  it.skip("marks key as validated with timestamp", () => {
    // Wave 0 stub — implementation in Plan 03
  });
});
```

**`__tests__/lib/hooks/useLLMGate.test.ts`** (Plan 03):
```typescript
import { describe, it } from "vitest";

describe("useLLMGate", () => {
  it.skip("returns canUseLLM=false for anonymous users", () => {
    // Wave 0 stub — implementation in Plan 03
  });

  it.skip("returns canUseLLM=false when budget exhausted", () => {
    // Wave 0 stub — implementation in Plan 03
  });

  it.skip("returns canUseLLM=false when not configured", () => {
    // Wave 0 stub — implementation in Plan 03
  });

  it.skip("returns correct roleLimitLabel for each role", () => {
    // Wave 0 stub — implementation in Plan 03
  });
});
```

Ensure both files are valid TypeScript that Vitest can collect.
  </action>
  <verify>
    <automated>npx vitest run __tests__/lib/stores/byoKeyStore.test.ts __tests__/lib/hooks/useLLMGate.test.ts --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>2 frontend test stub files exist, all discoverable by Vitest, all tests marked with it.skip.</done>
</task>

</tasks>

<verification>
- `cd ../ontokit-api && uv run pytest tests/unit/test_llm_*.py --collect-only -q` discovers all 12 backend test stubs
- `npx vitest run __tests__/lib/stores/byoKeyStore.test.ts __tests__/lib/hooks/useLLMGate.test.ts` discovers all 8 frontend test stubs
- All tests are skipped (not failing, not passing — just skipped)
</verification>

<success_criteria>
- All 7 test stub files exist at the paths specified in VALIDATION.md Wave 0 Requirements
- Backend stubs are collected by pytest without import errors
- Frontend stubs are collected by Vitest without import errors
- All tests use skip markers with descriptive reasons referencing the implementing plan
</success_criteria>

<output>
After completion, create `.planning/phases/11-roles-llm-abstraction-cost-controls/11-00-SUMMARY.md`
</output>
