---
phase: 11
slug: roles-llm-abstraction-cost-controls
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-06
---

# Phase 11 — UI Design Contract

> Visual and interaction contract for the Roles, LLM Abstraction & Cost Controls phase.
> Generated by gsd-ui-researcher. Consumed by gsd-planner, gsd-executor, gsd-ui-auditor.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (custom Tailwind + hand-rolled components) |
| Preset | not applicable |
| Component library | Custom — `components/ui/button.tsx`, `components/ui/dialog.tsx`, `components/ui/confirm-dialog.tsx` |
| Icon library | lucide-react (already installed) |
| Font | Inter via CSS variable `--font-inter`; monospace via `--font-mono` |

Source: `tailwind.config.ts`, `components/ui/`, `app/globals.css`

No `components.json` found. Project uses a bespoke component system with Tailwind CSS 3. The `EmbeddingSettingsSection` in `app/projects/[id]/settings/page.tsx` is the canonical reference pattern for all new settings sections in this phase.

---

## Spacing Scale

Declared values (multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline label padding, progress bar inner padding |
| sm | 8px | Compact row spacing, form element inner padding |
| md | 16px | Default element spacing, section content padding |
| lg | 24px | Section padding (`p-6` = 24px, matching existing settings sections) |
| xl | 32px | Layout gaps between settings section cards (`mb-8` = 32px) |
| 2xl | 48px | Major section breaks on the settings page |
| 3xl | 64px | Page-level spacing (not used in this phase) |

Exceptions: 44px minimum touch target for the BYO key toggle and the "Can self-merge structural PRs" toggle (accessibility requirement, WCAG 2.5.5).

Source: Spacing inferred from `mb-8 rounded-lg border border-slate-200 bg-white p-6` pattern repeated across 10+ settings sections. 8-point scale confirmed.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (`text-sm`) | 400 (regular) | 1.5 |
| Label | 14px (`text-sm`) | 500 (`font-medium`) | 1.4 |
| Heading (section) | 18px (`text-lg`) | 600 (`font-semibold`) | 1.2 |
| Metadata / caption | 12px (`text-xs`) | 500 (`font-medium`) | 1.4 |

Source: Settings page uses `text-lg font-semibold` for section headings, `text-sm font-medium` for field labels, `text-sm` for body copy, `text-xs font-medium` for stat labels. Pattern confirmed in lines 891–995 of `settings/page.tsx`.

No display-size type (20px+) is used in this phase — this is a settings-and-dashboard phase, not a marketing page.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `white` / `slate-950` (dark) | Page background, section card surfaces |
| Secondary (30%) | `slate-50` / `slate-800` (dark) | Card interiors, form fields, table rows |
| Accent (10%) | `primary-500` (#0ea5e9) | Primary CTA buttons, active provider card border, progress bar fill, coverage bar fill, active toggle |
| Destructive | `red-600` | Danger button variant only — used for budget exhaustion hard-stop actions |

Accent reserved for:
- "Save AI Settings" button (primary CTA)
- Active LLM provider card border + ring (`border-primary-500 ring-1 ring-primary-500`)
- Budget consumption progress bar fill
- BYO key validation spinner
- Active "Can self-merge structural PRs" toggle fill (`peer-checked:bg-primary-600`)

Accent is NOT used for: table rows, stat labels, budget warning states, section headings, or the budget exhaustion banner.

Semantic state colors (informational — not in the 60/30/10 split):
- Budget warning (>80% consumed): `amber-600` text, `amber-50` background, `amber-200` border
- Budget exhausted: `red-600` text, `red-50` background, `red-200` border
- Key validated successfully: `green-600` text, `green-50` background
- Key validation failed: `red-600` text, `red-50` background
- Usage normal: `slate-500` text (no tint background)

Source: Tailwind config (`primary-500: #0ea5e9`), existing settings page patterns (`bg-green-50`, `bg-red-50`, `bg-amber-50`), `button.tsx` variant definitions.

Dark mode: All surfaces use `dark:bg-slate-800` / `dark:border-slate-700`. All body copy uses `dark:text-slate-400`. All headings use `dark:text-white`. Confirmed from globals.css and existing settings sections.

---

## Component Inventory

All new components must follow existing codebase patterns. No new component primitives are introduced.

### New Components (this phase)

| Component | Path | Pattern Source |
|-----------|------|----------------|
| `LLMSettingsSection` | `components/projects/LLMSettingsSection.tsx` | Mirror `EmbeddingSettingsSection` exactly — provider card grid, API key field, save button |
| `LLMUsageSection` | `components/projects/LLMUsageSection.tsx` | Summary bar (budget used/remaining/burn rate) + sortable per-user table; no charts |
| `BYOKeyPopover` | `components/projects/BYOKeyPopover.tsx` | Inline popover anchored to the triggering LLM action button; 280px max-width |
| `LLMBudgetBanner` | `components/editor/LLMBudgetBanner.tsx` | Slim horizontal bar in editor header; `text-sm`, amber/red tinted; dismissible |
| `LLMRoleBadge` | `components/editor/LLMRoleBadge.tsx` | Inline `<span>` badge; `rounded-full px-2 py-0.5 text-xs font-medium`; shown near LLM affordances |

### Modified Components (this phase)

| Component | Path | Change |
|-----------|------|--------|
| `MemberList` | `components/projects/member-list.tsx` | Add "Can self-merge structural PRs" toggle per editor-role member row |
| Settings page | `app/projects/[id]/settings/page.tsx` | Add `LLMSettingsSection` and `LLMUsageSection` in a new "AI / LLM" section block |
| Editor header (developer layout) | `components/editor/developer/DeveloperEditorLayout.tsx` | Mount `LLMBudgetBanner` and `LLMRoleBadge` |
| Editor header (standard layout) | `components/editor/standard/StandardEditorLayout.tsx` | Mount `LLMBudgetBanner` and `LLMRoleBadge` |

---

## Screen Areas & Layout

### Project Settings Page — New AI / LLM Section

The new section is placed directly below the "Intelligence Features (Embeddings)" section and above the Danger Zone. It uses the identical container: `mb-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-700 dark:bg-slate-800`.

Section heading: "AI / LLM" (`text-lg font-semibold`) with a badge `rounded-full bg-primary-100 px-2 py-0.5 text-xs font-medium text-primary-700` reading "AI".

Visible to: owner and admin roles only (gate with `isOwner || canManage`).

#### LLMSettingsSection layout (vertical stack, `space-y-4`):

1. Section description paragraph (`text-sm text-slate-500`) — "Configure the LLM provider for AI-assisted suggestions on this project."
2. Provider grid: `grid gap-2 sm:grid-cols-2` — one card per provider. Each card: `rounded-lg border p-3 text-left transition-all`. Selected state: `border-primary-500 bg-primary-50 ring-1 ring-primary-500`. Unselected: `border-slate-200 hover:border-slate-300`.
   - Card content: provider logo (16x16px SVG or lucide icon) + provider name (`text-sm font-medium`) + model tier label (`text-xs text-slate-500`).
3. API key input (hidden when provider is Ollama/local): `<label>` (`text-sm font-medium`) + `<input type="password">` + show/hide toggle (Eye/EyeOff icon from lucide-react).
4. Local endpoint URL input (shown only when provider is Ollama): `<label>` + `<input type="url" placeholder="http://localhost:11434">`.
5. Default model tier radio group: "Quality" | "Cheap" — two `<label>` + `<input type="radio">` pairs in a horizontal flex row.
6. BYO key inline toggle: labeled "Use my own API key (BYO)" — checkbox or toggle. When enabled, shows inline explanatory text: "Your key is stored in your browser only and billed directly to you."
7. Key validation status row: shown after save attempt or key entry. States: validating (spinner), valid (CheckCircle green), invalid (XCircle red + message).
8. Save button: `Button` variant="primary" size="md" — "Save AI Settings". Loading state: spinner + "Saving…" text.

#### LLMUsageSection layout:

Placed as a second sub-section within the AI / LLM card, separated by a `<hr className="border-slate-200 dark:border-slate-700">`.

1. Sub-heading "Usage & Budget" (`text-base font-semibold`).
2. Budget ceiling input: `<label>` "Monthly budget ceiling (USD)" + `<input type="number" min="0" step="1">` + helper text "Leave blank for no cap."
3. Daily sub-cap input: `<label>` "Daily sub-cap (USD, optional)" + `<input type="number" min="0" step="1">`.
4. Summary bar (horizontal flex row): three stat tiles side-by-side.
   - "Budget used": dollar amount + month label (`text-xs text-slate-500`)
   - "Remaining": dollar amount + `text-green-600` when positive, `text-red-600` when zero/negative
   - "Burn rate": "~$X.XX/day" (`text-slate-500`)
5. Budget progress bar: `h-2 rounded-full bg-slate-200` container; `h-full rounded-full bg-primary-500` fill (amber when >80%, red when >100%).
6. Per-user call count table:
   - Columns: User, Calls today, Calls this month, Est. cost (USD), BYO key
   - `text-xs font-medium text-slate-500` column headers
   - `text-sm` cell text
   - Pagination: "Showing N of M users" + Previous / Next buttons if >20 rows
   - Default sort: calls this month descending

### Editor Header — Budget Banner & Role Badge

`LLMBudgetBanner`: Slim bar spanning full editor header width, below the existing toolbar row. Only rendered when `budgetExhausted === true` OR `budgetConsumedPct >= 0.8`. Not rendered otherwise.

- Warning state (80-99%): `bg-amber-50 border-b border-amber-200 text-amber-700 dark:bg-amber-900/20 dark:text-amber-400` — text: "AI budget {X}% used this month. You have ${Y} remaining."
- Exhausted state (100%): `bg-red-50 border-b border-red-200 text-red-700 dark:bg-red-900/20 dark:text-red-400` — text: "AI budget exhausted for this month. LLM features are disabled. Manual suggestions continue to work."
- Layout: `flex items-center justify-between px-4 py-1.5 text-sm`. Dismiss button (X icon, `ghost` variant, `size="sm"`) at right edge.

`LLMRoleBadge`: Inline `<span>` placed in the editor toolbar, near the future LLM action buttons. Not rendered if user has no LLM access.

- Admin: `bg-violet-100 text-violet-700 dark:bg-violet-900/30 dark:text-violet-400` — "Admin — unlimited"
- Editor: `bg-blue-100 text-blue-700 dark:bg-blue-900/30 dark:text-blue-400` — "Editor — 500/day"
- Suggester: `bg-green-100 text-green-700 dark:bg-green-900/30 dark:text-green-400` — "Suggester — 100/day"
- Layout: `rounded-full px-2 py-0.5 text-xs font-medium`

### BYO Key Popover

Triggered on first LLM action click when no BYO key is stored and the project has no project-level key set.

- Anchored below the triggering button, 8px gap.
- Width: 280px fixed.
- Container: `rounded-lg border border-slate-200 bg-white shadow-lg p-4 dark:border-slate-700 dark:bg-slate-800`.
- Heading: "Enter your API key" (`text-sm font-semibold`).
- Body: "Your key stays in your browser and is billed directly to you." (`text-xs text-slate-500`).
- Input: `<input type="password" placeholder="sk-…">` full width.
- Actions row: "Use Key" (primary small button) + "Settings" link (text link, `text-primary-600 text-xs`).
- Dismiss: click outside or Escape key.

### Member List — Self-Merge Toggle

For each member row where `role === "editor"`:
- Append a trailing cell: toggle labeled "Structural self-merge" — same toggle markup as the existing auto-embed toggle (`peer h-5 w-9 rounded-full bg-slate-300 peer-checked:bg-primary-600`).
- Default: off (`can_self_merge_structural: false`).
- Toggle is not shown for viewer, suggester, admin, or owner rows.

### Admin Self-Merge Confirmation Dialog

When an admin clicks "Merge" on a structural PR:
- Uses existing `components/ui/confirm-dialog.tsx` pattern.
- Title: "Merge directly to main?"
- Body: "You are about to merge this structural change without peer review. This cannot be undone."
- Confirm button: `Button` variant="danger" — "Merge directly"
- Cancel button: `Button` variant="outline" — "Cancel"

---

## Interaction States

### LLM Action Buttons (foundation contract for Phases 12-14)

This phase establishes the disabled state that future LLM buttons must respect:

| State | Visual |
|-------|--------|
| LLM available | Normal enabled state (defined in Phases 12-14) |
| Budget exhausted | `disabled:opacity-50 disabled:pointer-events-none` + tooltip "LLM budget exhausted for this month" on hover |
| Provider not configured | `disabled:opacity-50` + tooltip "AI provider not configured. Go to Project Settings." |
| Rate limit reached | `disabled:opacity-50` + tooltip "Daily LLM limit reached (500/day). Resets at midnight UTC." |
| No LLM access (anonymous/viewer) | Button not rendered — no affordance shown |

Tooltip: `title` attribute only for this phase (full tooltip component in Phase 14).

### Provider Card Selection

- Click selects provider: immediate visual update (no debounce).
- Selection does not auto-save — user must click "Save AI Settings".
- Only one card selected at a time.

### Key Validation Flow

1. User enters API key in settings or BYO popover.
2. On blur or "Save AI Settings" click: backend validates key with a lightweight provider test call.
3. During validation: spinner in key row (`h-4 w-4 animate-spin`).
4. On success: `CheckCircle` icon (green) + "Key valid" text.
5. On failure: `XCircle` icon (red) + specific error ("Invalid key", "Provider unreachable", "Insufficient permissions").
6. Validation result clears after 5 seconds or on next key edit.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Save AI Settings" |
| Empty state (no LLM config) | "No AI provider configured" / "Add a provider to enable LLM-assisted suggestions for all project members." |
| Empty state (usage, no calls yet) | "No LLM calls recorded this month." |
| Error — key invalid | "This API key was rejected by [Provider]. Check that it's correct and has the right permissions." |
| Error — provider unreachable | "Could not reach [Provider]. Check your endpoint URL and try again." |
| Error — save failed | "Settings could not be saved. Try again, or reload the page if the problem continues." |
| Budget exhaustion banner | "AI budget exhausted for this month. LLM features are disabled. Manual suggestions continue to work." |
| Budget warning banner | "AI budget {X}% used this month. You have ${Y} remaining." |
| BYO popover heading | "Enter your API key" |
| BYO popover body | "Your key stays in your browser and is billed directly to you." |
| BYO key saved | "Key saved in this browser. It won't be sent to our servers." |
| BYO key cleared | "API key cleared from this browser." |
| Self-merge confirmation title | "Merge directly to main?" |
| Self-merge confirmation body | "You are about to merge this structural change without peer review. This cannot be undone." |
| Self-merge confirm button | "Merge directly" |
| Rate limit tooltip | "Daily LLM limit reached ({N}/day). Resets at midnight UTC." |
| Provider not configured tooltip | "AI provider not configured. Go to Project Settings." |
| Budget exhausted tooltip | "LLM budget exhausted for this month." |
| LLM role badge — admin | "Admin — unlimited" |
| LLM role badge — editor | "Editor — 500/day" |
| LLM role badge — suggester | "Suggester — 100/day" |

Destructive actions in this phase:

| Action | Confirmation approach |
|--------|----------------------|
| Admin merges structural PR directly | `confirm-dialog.tsx` with danger button; one extra click |
| Clear BYO key from browser | Inline "Clear key" link; no dialog (reversible by re-entering key) |

---

## Accessibility Contract

- All form inputs have explicit `<label>` elements — no `placeholder` as sole label.
- Provider cards are `<button>` elements (not `<div>`) — keyboard navigable.
- Toggle for "Can self-merge structural PRs" uses `<input type="checkbox">` with visible `<label>`.
- BYO key popover traps focus while open; Escape closes it.
- Budget banner has `role="alert"` when state transitions from no-banner to exhausted (assertive announcement via `useAnnounce()`).
- LLMRoleBadge has `aria-label="Your LLM access: {role}, {limit}"`.
- All icon-only buttons (dismiss X, show/hide key) have `aria-label`.
- Touch targets for toggles: minimum 44px hit area (wrapper `<label>` with `min-h-[44px]`).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — no shadcn |
| third-party | none | not applicable |

No third-party component registries are used in this phase. All components are custom-built following existing codebase patterns.

---

## Implementation Notes for Executor

1. **Settings section container:** `<section className="mb-8 rounded-lg border border-slate-200 bg-white p-6 dark:border-slate-700 dark:bg-slate-800">` — copy verbatim from `EmbeddingSettingsSection`.
2. **Provider logo assets:** Use lucide-react icons as placeholders — `Cpu` for local/Ollama, `Sparkles` for OpenAI, `Bot` for Anthropic, `Star` for Google, `Zap` for Cohere, `Globe` for generic. Do not fetch external SVG assets.
3. **localStorage key for BYO store:** `ontokit-byo-key` (Zustand persist key). Structure: `{ provider: string; key: string; validatedAt: string | null }`.
4. **Budget progress bar thresholds:** `consumedPct < 0.8` → `bg-primary-500`; `0.8 ≤ consumedPct < 1.0` → `bg-amber-500`; `consumedPct >= 1.0` → `bg-red-500`.
5. **Usage table date range default:** Current calendar month. No date picker in this phase.
6. **BYO popover placement:** Use CSS `position: absolute` with `top: 100%; left: 0; mt-2` relative to the triggering button's container. No Radix Popover — keep it simple.
7. **Price-per-token table update:** Backend-managed config file (no admin UI for prices in this phase — deferred to Claude's discretion).
8. **`canSelfMergeStructural` toggle placement:** Right edge of each editor-role member row, after the existing role dropdown.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
