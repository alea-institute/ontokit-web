---
phase: 12-toolchain-integration-duplicate-detection
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - ../ontokit-api/pyproject.toml
  - ../ontokit-api/uv.lock
  - ../ontokit-api/tests/unit/test_structural_similarity.py
  - ../ontokit-api/tests/unit/test_duplicate_check.py
  - ../ontokit-api/tests/unit/test_embedding_rebuild.py
  - ../ontokit-api/tests/unit/test_reasoner_validation.py
autonomous: true
requirements: [TOOL-01, TOOL-02, TOOL-03, TOOL-04, TOOL-05, DEDUP-01, DEDUP-02, DEDUP-03, DEDUP-04, DEDUP-05, DEDUP-06, DEDUP-07, DEDUP-08]

must_haves:
  truths:
    - "folio-python is importable from the ontokit-api virtualenv"
    - "All 4 test stub files exist and are discoverable by pytest"
  artifacts:
    - path: "../ontokit-api/tests/unit/test_structural_similarity.py"
      provides: "Stubs for TOOL-01, TOOL-02 structural + gloss tests"
      contains: "pytest.mark.skip"
    - path: "../ontokit-api/tests/unit/test_duplicate_check.py"
      provides: "Stubs for DEDUP-04 through DEDUP-08 composite scoring tests"
      contains: "pytest.mark.skip"
    - path: "../ontokit-api/tests/unit/test_embedding_rebuild.py"
      provides: "Stubs for DEDUP-01, DEDUP-02, DEDUP-03 rebuild + index tests"
      contains: "pytest.mark.skip"
    - path: "../ontokit-api/tests/unit/test_reasoner_validation.py"
      provides: "Stubs for TOOL-03, TOOL-04 reasoner tests"
      contains: "pytest.mark.skip"
  key_links: []
---

<objective>
Install folio-python dependency and create Wave 0 test stubs for all Phase 12 services.

Purpose: Nyquist compliance — every downstream plan has a pre-existing test file to run against. folio-python must be in the venv before any structural similarity code can import it.
Output: 4 test stub files + updated pyproject.toml with folio-python dependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-toolchain-integration-duplicate-detection/12-CONTEXT.md
@.planning/phases/12-toolchain-integration-duplicate-detection/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install folio-python and create test stubs</name>
  <files>
    ../ontokit-api/pyproject.toml
    ../ontokit-api/uv.lock
    ../ontokit-api/tests/unit/test_structural_similarity.py
    ../ontokit-api/tests/unit/test_duplicate_check.py
    ../ontokit-api/tests/unit/test_embedding_rebuild.py
    ../ontokit-api/tests/unit/test_reasoner_validation.py
  </files>
  <read_first>
    ../ontokit-api/pyproject.toml
    ../ontokit-api/tests/unit/test_llm_config.py
    ../ontokit-api/tests/unit/test_duplicate_detection.py
  </read_first>
  <action>
1. **Install folio-python** (TOOL-05): Run `cd ../ontokit-api && uv add folio-python` to add folio-python to the project dependencies. Verify it appears in pyproject.toml under [project.dependencies].

2. **Create test_structural_similarity.py** — stubs for Plans 02 (TOOL-01, TOOL-02):
   - `test_structural_similarity_returns_jaccard_score` — skip stub for Jaccard similarity between parent sets
   - `test_structural_similarity_unknown_iri_returns_zero` — skip stub for graceful degradation
   - `test_structural_similarity_same_entity_returns_one` — skip stub for self-similarity
   - `test_gloss_extraction_stub_raises_not_implemented` — skip stub for OpenGloss (TOOL-02)
   Follow the Phase 11 stub pattern: `@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 02")`

3. **Create test_duplicate_check.py** — stubs for Plan 04 (DEDUP-04 through DEDUP-08):
   - `test_exact_label_match_returns_block_verdict` — composite >0.95 blocks (DEDUP-05)
   - `test_semantic_similarity_warn_range` — composite 0.80-0.95 warns (DEDUP-06)
   - `test_below_threshold_passes_silently` — composite <=0.80 passes (DEDUP-07)
   - `test_composite_score_weights` — exact=40%, semantic=40%, structural=20% (DEDUP-04, D-01)
   - `test_all_branch_scope` — search spans all branches (DEDUP-08)
   - `test_rejection_history_surfaced` — rejected candidates show rejection reason (D-09)
   - `test_response_includes_score_breakdown` — response has verdict, composite_score, score_breakdown, candidates (D-13)
   Skip reason: `"Wave 0 stub — implementation in Plan 04"`

4. **Create test_embedding_rebuild.py** — stubs for Plans 01+03 (DEDUP-01, DEDUP-02, DEDUP-03):
   - `test_hnsw_index_creation_migration` — HNSW index exists on entity_embeddings (DEDUP-02)
   - `test_all_branch_embedding_query` — semantic search across all branches (DEDUP-01)
   - `test_webhook_triggers_rebuild_job` — POST webhook enqueues ARQ job (DEDUP-03)
   - `test_startup_freshness_check` — stale index triggers background rebuild (D-05)
   Skip reason: `"Wave 0 stub — implementation in Plans 01/03"`

5. **Create test_reasoner_validation.py** — stubs for Plan 02 (TOOL-03, TOOL-04):
   - `test_reasoner_loads_owl_file` — owlready2 loads ontology (TOOL-03)
   - `test_reasoner_detects_cycle` — cycle detection via OWL reasoner (TOOL-03)
   - `test_pre_commit_validation_endpoint` — POST endpoint returns validation results (TOOL-04)
   Skip reason: `"Wave 0 stub — implementation in Plan 02"`
  </action>
  <verify>
    <automated>cd ../ontokit-api && python -c "import folio" && python -m pytest tests/unit/test_structural_similarity.py tests/unit/test_duplicate_check.py tests/unit/test_embedding_rebuild.py tests/unit/test_reasoner_validation.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "folio-python" ../ontokit-api/pyproject.toml` succeeds
    - `python -c "from folio.graph import FOLIO"` succeeds in ontokit-api venv
    - `pytest tests/unit/test_structural_similarity.py --co -q` shows 4 tests collected
    - `pytest tests/unit/test_duplicate_check.py --co -q` shows 7 tests collected
    - `pytest tests/unit/test_embedding_rebuild.py --co -q` shows 4 tests collected
    - `pytest tests/unit/test_reasoner_validation.py --co -q` shows 3 tests collected
    - All test files contain `@pytest.mark.skip` decorators
  </acceptance_criteria>
  <done>folio-python is importable in the ontokit-api venv, all 18 test stubs are discoverable by pytest, and all are skip-marked pending implementation.</done>
</task>

</tasks>

<verification>
- `cd ../ontokit-api && python -c "from folio.graph import FOLIO; print('OK')"` prints OK
- `cd ../ontokit-api && python -m pytest tests/unit/test_structural_similarity.py tests/unit/test_duplicate_check.py tests/unit/test_embedding_rebuild.py tests/unit/test_reasoner_validation.py -v --tb=short` — all 18 tests collected, all skipped
</verification>

<success_criteria>
- folio-python appears in pyproject.toml dependencies
- All 4 test files exist under tests/unit/
- All tests are skip-marked and discoverable
- No import errors in any test file
</success_criteria>

<output>
After completion, create `.planning/phases/12-toolchain-integration-duplicate-detection/12-00-SUMMARY.md`
</output>
