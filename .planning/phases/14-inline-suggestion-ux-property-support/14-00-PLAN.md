---
phase: 14-inline-suggestion-ux-property-support
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - __tests__/lib/stores/suggestionStore.test.ts
  - __tests__/lib/hooks/useSuggestions.test.ts
  - __tests__/lib/api/generation.test.ts
autonomous: true
requirements: [UX-04, UX-05, UX-06]

must_haves:
  truths:
    - "Test stubs for suggestion store exist and are importable by vitest"
    - "Test stubs for useSuggestions hook exist and are importable by vitest"
    - "Test stubs for generation API client exist and are importable by vitest"
  artifacts:
    - path: "__tests__/lib/stores/suggestionStore.test.ts"
      provides: "Suggestion store unit test stubs"
      contains: "describe"
    - path: "__tests__/lib/hooks/useSuggestions.test.ts"
      provides: "useSuggestions hook test stubs"
      contains: "describe"
    - path: "__tests__/lib/api/generation.test.ts"
      provides: "Generation API client test stubs"
      contains: "describe"
  key_links: []
---

<objective>
Create test stub files for all Phase 14 Zustand stores, hooks, and API clients so that downstream plans can verify against real test infrastructure without discovery failures (Nyquist compliance).

Purpose: Ensures `npm run test -- --run` discovers all Phase 14 test files from Wave 1 onward.
Output: Three test stub files with `@pytest.mark.skip`-equivalent `it.skip()` placeholders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-inline-suggestion-ux-property-support/14-RESEARCH.md
@.planning/phases/14-inline-suggestion-ux-property-support/14-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 test stubs for suggestion store, hook, and API client</name>
  <files>
    __tests__/lib/stores/suggestionStore.test.ts,
    __tests__/lib/hooks/useSuggestions.test.ts,
    __tests__/lib/api/generation.test.ts
  </files>
  <read_first>
    __tests__/lib/stores/draftStore.test.ts,
    __tests__/lib/stores/editorModeStore.test.ts,
    vitest.config.ts
  </read_first>
  <action>
Create three test stub files following the existing test patterns in `__tests__/lib/stores/`.

1. **`__tests__/lib/stores/suggestionStore.test.ts`** — Zustand store for suggestion state (UX-05, UX-06):
   ```typescript
   import { describe, it, expect } from "vitest";

   describe("suggestionStore", () => {
     it.skip("setSuggestions stores suggestions keyed by entityIri::suggestionType", () => {
       expect(true).toBe(true);
     });

     it.skip("acceptSuggestion changes status to accepted", () => {
       expect(true).toBe(true);
     });

     it.skip("rejectSuggestion changes status to rejected", () => {
       expect(true).toBe(true);
     });

     it.skip("editSuggestion sets editedValue on the stored suggestion", () => {
       expect(true).toBe(true);
     });

     it.skip("getPendingCount returns count of suggestions with status pending", () => {
       expect(true).toBe(true);
     });

     it.skip("clearSuggestions removes all suggestions for a given entityIri", () => {
       expect(true).toBe(true);
     });

     it.skip("clearAllSuggestions empties the entire store", () => {
       expect(true).toBe(true);
     });
   });
   ```

2. **`__tests__/lib/hooks/useSuggestions.test.ts`** — Core suggestion lifecycle hook:
   ```typescript
   import { describe, it, expect } from "vitest";

   describe("useSuggestions", () => {
     it.skip("request() calls generationApi.generateSuggestions and stores results", () => {
       expect(true).toBe(true);
     });

     it.skip("returns isLoading=true while API call is in flight", () => {
       expect(true).toBe(true);
     });

     it.skip("returns error string when API call fails", () => {
       expect(true).toBe(true);
     });

     it.skip("accept() calls onAccepted callback with suggestion and editedValue", () => {
       expect(true).toBe(true);
     });

     it.skip("reject() sets suggestion status to rejected in store", () => {
       expect(true).toBe(true);
     });
   });
   ```

3. **`__tests__/lib/api/generation.test.ts`** — Generation API client:
   ```typescript
   import { describe, it, expect } from "vitest";

   describe("generationApi", () => {
     it.skip("generateSuggestions sends POST with correct path and body", () => {
       expect(true).toBe(true);
     });

     it.skip("generateSuggestions includes X-BYO-API-Key header when byoKey provided", () => {
       expect(true).toBe(true);
     });

     it.skip("generateSuggestions returns GenerateSuggestionsResponse shape", () => {
       expect(true).toBe(true);
     });
   });
   ```

Create the `__tests__/lib/hooks/` directory if it does not exist (the `__tests__/lib/stores/` directory already exists).
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/ontokit-web" && npm run test -- --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `__tests__/lib/stores/suggestionStore.test.ts` exists and contains `describe("suggestionStore"`
    - File `__tests__/lib/hooks/useSuggestions.test.ts` exists and contains `describe("useSuggestions"`
    - File `__tests__/lib/api/generation.test.ts` exists and contains `describe("generationApi"`
    - All three files contain `it.skip` stubs (not `it(` without skip)
    - `npm run test -- --run` passes (all stubs skipped, no failures)
  </acceptance_criteria>
  <done>All three test stub files exist, are discovered by vitest, and the full test suite passes with skipped stubs.</done>
</task>

</tasks>

<verification>
npm run test -- --run
All existing tests pass, three new test files discovered with skipped stubs.
</verification>

<success_criteria>
- Three test stub files created in `__tests__/lib/`
- `npm run test -- --run` exits 0
- No test failures introduced
</success_criteria>

<output>
After completion, create `.planning/phases/14-inline-suggestion-ux-property-support/14-00-SUMMARY.md`
</output>
