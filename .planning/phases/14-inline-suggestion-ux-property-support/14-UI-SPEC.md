---
phase: 14
slug: inline-suggestion-ux-property-support
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-07
---

# Phase 14 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (custom component library) |
| Preset | not applicable |
| Component library | Radix UI primitives (@radix-ui/react-dialog, @radix-ui/react-tabs, @radix-ui/react-tooltip, @radix-ui/react-scroll-area) |
| Icon library | lucide-react v1.7.0 — use `Sparkles` for AI-generated content, `Check` for accept, `X` for reject, `Pencil` for edit, `ChevronLeft`/`ChevronRight` for Prev/Next, `Loader2` for loading spinner |
| Font | Inter (variable: `--font-inter`) + JetBrains Mono for code/IRI display |

**Source:** `app/globals.css` `@theme` block, `app/layout.tsx`, existing `components/ui/`, `package.json`.

No `components.json` found. This project uses hand-rolled components following Tailwind CSS v4 utility patterns with Radix primitives. shadcn gate: not applicable.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps (`gap-1`), badge inline padding (`px-1 py-0.5`) |
| sm | 8px | Compact element spacing (`gap-2`), annotation row internal gaps |
| md | 16px | Default panel padding (`px-4`), section padding (`p-4`) |
| lg | 24px | Panel section vertical rhythm (`py-6`) |
| xl | 32px | Major section breaks between suggestion groups |
| 2xl | 48px | Reserved for page-level spacing (not used in editor panels) |
| 3xl | 64px | Not used in this phase |

Exceptions:
- Suggestion card icon buttons: 6px (`p-1.5`) to match existing `px-2.5 py-1.5` row button pattern
- Inline scope toggle (This class / Siblings / Descendants): 6px vertical padding (`py-1.5`) for touch target comfort
- Next/Prev navigation buttons in detail panel header: `px-2.5 py-1.5` to match existing header action button sizing

**Source:** Observed from `components/editor/ClassDetailPanel.tsx` and `components/ui/button.tsx` (`h-8 px-3` for `sm` size).

---

## Typography

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 14px (`text-sm`) | 400 (normal) | 1.5 | Annotation values, suggestion card text, error messages |
| Label | 12px (`text-xs`) | 500 (medium) | 1.4 | Section headers, confidence badges, IRI truncation, scope toggle options |
| Heading | 18px (`text-lg`) | 600 (semibold) | 1.2 | Class/property name in detail panel header |
| Micro | 9px (`text-[9px]`) | 700 (bold) | 1.0 | Entity type badge letters (existing "C" for Class) |

**Source:** Observed directly from `components/editor/ClassDetailPanel.tsx` lines 544–605. Pattern: `text-lg font-semibold` for entity name, `text-sm` for content rows, `text-xs font-medium` for action buttons and badges.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `bg-white` / `dark:bg-slate-900` (`rgb(var(--background-start-rgb))`) | Main panel background, editor surfaces |
| Secondary (30%) | `bg-slate-50` / `dark:bg-slate-800` | Suggestion card backgrounds, tree panel, section dividers |
| Accent (10%) | `primary-500` (#0ea5e9) / `primary-600` (#0284c7) | Accept button, active tree selection, focus rings, "Suggest Improvements" trigger button |
| AI/Sparkle accent | `amber-500` / `amber-100 bg` | Sparkle icon color, confidence badge tint for 70–89%, pending count badge in editor header |
| Destructive | `red-600` / `red-50 bg` | Reject button (icon only, no fill), error banners, validation failure messages |

Accent reserved for:
1. The "Suggest Improvements" button (primary variant)
2. Accept action icon button on suggestion cards (check icon, `text-primary-600`)
3. Active/selected tree item background
4. Focus rings on all interactive elements
5. Pending suggestion count badge border

Confidence color coding (from D-03):
- 90–100%: `text-green-700 bg-green-100 dark:text-green-400 dark:bg-green-900/30`
- 70–89%: `text-amber-700 bg-amber-100 dark:text-amber-400 dark:bg-amber-900/30`
- Below 70%: `text-red-700 bg-red-100 dark:text-red-400 dark:bg-red-900/20`

**Source:** `app/globals.css` `@theme` block for primary palette; `components/ui/button.tsx` for variant colors; `components/editor/AutoSaveAffordanceBar.tsx` for amber/green/red status semantics; `components/editor/LLMRoleBadge.tsx` for badge pattern.

---

## Component Inventory

### New Components (Phase 14)

#### `SuggestionCard`
Inline card rendered below each section of `ClassDetailPanel` and the property detail panel. Follows the same row anatomy as `AnnotationRow`.

**Anatomy:**
```
[sparkle icon] [content] [confidence badge] [accept ✓] [reject ✗] [edit ✎]
```

- Container: `flex items-start gap-2 rounded-md border border-dashed border-slate-200 bg-slate-50 px-3 py-2 dark:border-slate-700 dark:bg-slate-800/50`
- Sparkle marker: `<Sparkles className="h-3 w-3 shrink-0 text-amber-500 mt-0.5" />` — always present, visually distinguishes AI content
- Content text: `text-sm text-slate-700 dark:text-slate-300` (same as existing annotation row values)
- Confidence badge: `inline-flex items-center rounded-full px-1.5 py-0.5 text-xs font-medium` with color per confidence thresholds above
- Accept button: `rounded-sm p-1 text-primary-600 hover:bg-primary-50 dark:text-primary-400 dark:hover:bg-primary-900/20` + `<Check className="h-3.5 w-3.5" />`
- Reject button: `rounded-sm p-1 text-slate-400 hover:text-red-500 hover:bg-red-50 dark:hover:bg-red-900/20` + `<X className="h-3.5 w-3.5" />`
- Edit button: `rounded-sm p-1 text-slate-400 hover:text-slate-700 hover:bg-slate-100 dark:hover:bg-slate-700` + `<Pencil className="h-3.5 w-3.5" />`
- Edit mode (inline text field): `flex-1 rounded-md border border-slate-300 bg-white px-2.5 py-1.5 text-sm focus:border-primary-500 focus:outline-hidden focus:ring-1 focus:ring-primary-500 dark:border-slate-600 dark:bg-slate-700 dark:text-white`

Keyboard: Tab/Shift+Tab moves between cards, Enter accepts focused card, Backspace/Delete rejects, E opens edit mode.

#### `SuggestionSkeleton`
Placeholder shown while LLM request is in-flight. Uses existing `animate-pulse` pattern.

- Container: same dashed border as `SuggestionCard`
- Content line: `h-4 w-3/4 animate-pulse rounded-sm bg-slate-200 dark:bg-slate-700`
- Badge placeholder: `h-4 w-10 animate-pulse rounded-full bg-slate-200 dark:bg-slate-700`
- Show 3 skeleton cards per section while loading (matches UX for "where results will land")

#### `SuggestImprovementsButton`
Trigger button in each section header of ClassDetailPanel (and property detail panel).

- Variant: ghost + sparkle: `flex items-center gap-1.5 rounded-md px-2.5 py-1.5 text-xs font-medium text-primary-600 hover:bg-primary-50 dark:text-primary-400 dark:hover:bg-primary-900/20`
- Icon: `<Sparkles className="h-3 w-3" />`
- Label: "Suggest" (terse, fits in section header row without overflow)
- Disabled state when LLM budget exhausted: `opacity-50 cursor-not-allowed` — renders `LLMBudgetBanner` in its place

#### `SuggestionScopeToggle`
Inline radio-style toggle rendered next to the "Suggest improvements" button for annotation requests.

- Options: "This class" | "Siblings" | "Descendants"
- Layout: `inline-flex rounded-md border border-slate-200 dark:border-slate-700 text-xs`
- Active option: `bg-primary-100 text-primary-700 dark:bg-primary-900/30 dark:text-primary-300 font-medium`
- Inactive option: `text-slate-500 hover:bg-slate-50 dark:text-slate-400 dark:hover:bg-slate-800`
- Default: "This class" (narrowest scope, least surprising)

#### `PendingSuggestionBadge`
Shown in editor header next to the existing action group. Updates in real time.

- Container: `inline-flex items-center gap-1 rounded-full bg-amber-100 px-2 py-0.5 text-xs font-medium text-amber-800 dark:bg-amber-900/30 dark:text-amber-400`
- Icon: `<Sparkles className="h-3 w-3" />`
- Count: e.g. "5" — plain number, no label suffix (fits tight header)
- Tooltip on hover: "5 pending suggestions — click to scroll to first"
- Clicking: scrolls detail panel to first `SuggestionCard` with `focus()` triggered

#### `BranchNavigator` (Prev/Next)
Added to `ClassDetailPanel` header row when iterating a branch.

- Container: `flex items-center gap-1` appended to existing header action row
- Prev button: `rounded-sm p-1.5 text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-700 disabled:opacity-30` + `<ChevronLeft className="h-4 w-4" />`
- Next button: same + `<ChevronRight className="h-4 w-4" />`
- Position label: `text-xs text-slate-400` e.g. "3 / 12" between the two buttons
- aria-label: "Previous class" / "Next class"

#### `ClassesPropertiesTabBar`
Tab switcher at top of the tree panel (above search input). Replaces or precedes the existing search row.

- Uses `@radix-ui/react-tabs` (already in `package.json`)
- Tab list: `flex border-b border-slate-200 dark:border-slate-700`
- Tab trigger: `flex-1 px-3 py-2 text-xs font-medium text-slate-500 data-[state=active]:text-primary-600 data-[state=active]:border-b-2 data-[state=active]:border-primary-500 dark:text-slate-400 dark:data-[state=active]:text-primary-400`
- Labels: "Classes" | "Properties"

#### `SuggestionGroupSection`
Collapsible group for sibling/descendant scope results. Wraps suggestion cards under a class name header.

- Toggle: `<ChevronRight>` / `<ChevronDown>` pattern (matches existing `CrossReferencesPanel`)
- Header: `flex items-center gap-2 text-xs font-medium text-slate-700 dark:text-slate-300 py-1 cursor-pointer hover:text-slate-900`
- Count badge: `text-xs text-slate-400` — "(3 suggestions)"

### Modified Components (Phase 14)

#### `ClassDetailPanel` — section slot additions
Each section (Labels, Comments, Annotations, Children, Parents, Relationships) gains a suggestion slot below its existing content rows. The slot renders:
1. `SuggestImprovementsButton` in section header (right-aligned, via existing `flex justify-between` header row)
2. `SuggestionSkeleton` × 3 while loading
3. `SuggestionCard` × N when results arrive
4. Inline error banner on failure (see Copywriting)

#### Editor header — pending badge
`PendingSuggestionBadge` added to the right side of the existing editor header action group, between `LLMRoleBadge` and other controls. Renders only when `pendingCount > 0`.

#### Property detail panel — section adaptation
For `ObjectProperty`: "Domain" and "Range" sections replace "Children" section. Same `SuggestionCard` pattern applies; suggestions show proposed domain/range classes. Labels, annotations, relationships sections: identical to class panel.

---

## Interaction States

### Suggestion Card States

| State | Visual |
|-------|--------|
| Default | Dashed border, `bg-slate-50`, sparkle icon amber |
| Hover | `border-solid border-slate-300`, subtle shadow `shadow-sm` |
| Accept focused (keyboard) | `ring-2 ring-primary-500/50 ring-inset` |
| Reject focused (keyboard) | `ring-2 ring-red-400/50 ring-inset` |
| Editing | Text field replaces content text; accept/reject buttons hidden; "Save" + "Cancel" appear at right |
| Accepted (flash) | 200ms transition to `bg-green-50 border-green-200 dark:bg-green-900/10 dark:border-green-900/50` before card transitions to normal annotation row |
| Rejected (exit) | Card collapses with `max-height: 0` transition over 150ms, then removed from DOM |

### Loading States

| State | Visual |
|-------|--------|
| Suggestions loading | 3 `SuggestionSkeleton` cards with `animate-pulse`, section header shows `<Loader2 className="h-3 w-3 animate-spin" />` |
| LLM unavailable (no config) | Section header shows tooltip "Configure LLM in project settings" on hover of disabled `SuggestImprovementsButton` |
| Budget exhausted | `LLMBudgetBanner` (existing component) renders inline in section, replaces button |

### Animation Contract

All motion respects `@media (prefers-reduced-motion: reduce)` (set in `globals.css` to `0.01ms`).

- Accepted card flash: `transition-colors duration-200`
- Rejected card collapse: `transition-all duration-150 overflow-hidden`
- Suggestion group expand/collapse: `transition-all duration-150`
- Skeleton shimmer: `animate-pulse` (CSS only, no JS)
- Badge count update: no animation (number changes instantly)

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Suggest" (section header trigger button) |
| Full trigger label (tooltip) | "Get LLM suggestions for this section" |
| Empty state — no suggestions configured | "Configure an LLM provider in project settings to enable suggestions." |
| Empty state — no suggestions returned | "No suggestions — the LLM found no additions for this section." |
| Loading state | (skeleton cards — no text label needed; spinner in section header suffices) |
| Error — LLM failure | "Could not generate suggestions. [Retry]" |
| Error — budget exhausted | (handled by existing `LLMBudgetBanner` component — copy already defined) |
| Error — rate limit hit | "Daily limit reached (500 calls). Suggestions reset at midnight." |
| Error — validation block | "[Validation rule description] — this suggestion was blocked." |
| Accept action aria-label | "Accept suggestion" |
| Reject action aria-label | "Reject suggestion" |
| Edit action aria-label | "Edit suggestion before accepting" |
| Save edit action | "Accept" (replaces pencil icon button in edit mode) |
| Cancel edit action | "Cancel" |
| Pending badge tooltip | "{N} pending suggestion{s} — click to scroll to first" |
| Next navigation aria-label | "Next class in branch" |
| Prev navigation aria-label | "Previous class in branch" |
| Branch position label | "{current} / {total}" (e.g. "3 / 12") |
| Tab: classes | "Classes" |
| Tab: properties | "Properties" |
| Scope toggle options | "This class" \| "Siblings" \| "Descendants" |
| Suggestion group collapse label | "{N} suggestion{s}" |
| Accepted flash | (no text — visual only: green background flash) |

Destructive actions in this phase:
- **Reject suggestion:** Single click on X icon button. No confirmation dialog — rejection is reversible within the session (rejected cards are hidden but session state persists). aria-label confirms intent: "Reject suggestion".
- **Discard all suggestions (session level):** Handled by existing session discard flow (out of scope for this phase's new components — deferred to existing `useSuggestionSession` discard path).

**Source:** D-14, D-15 from `14-CONTEXT.md`; existing `LLMBudgetBanner` copy patterns.

---

## Accessibility Contract

All new components inherit the project's existing a11y baseline from Phase 6C:

- `SuggestionCard` icon buttons: `aria-label` required (no title attribute)
- `SuggestionCard` group: `role="list"` on container, `role="listitem"` on each card
- `SuggestionScopeToggle`: uses `role="radiogroup"` + `role="radio"` with `aria-checked`
- `BranchNavigator`: `aria-label` on both buttons; position label has `aria-live="polite"` so screen readers announce navigation
- `PendingSuggestionBadge`: `aria-live="polite"` region; announces count changes
- `ClassesPropertiesTabBar`: uses Radix Tabs (`role="tablist"`, `role="tab"`, `role="tabpanel"`) — built-in from `@radix-ui/react-tabs`
- Keyboard navigation category added to existing `KeyboardShortcutDialog` under heading "Suggestions":
  - Tab / Shift+Tab: Move between suggestion cards
  - Enter: Accept focused suggestion
  - Backspace or Delete: Reject focused suggestion
  - E: Edit focused suggestion

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — project does not use shadcn registry |
| third-party | none | not applicable |

No third-party component blocks are introduced. All new components are hand-rolled following existing project patterns. `@radix-ui/react-tabs` is already installed.

---

## Pre-Population Source Log

| Decision | Source |
|----------|--------|
| Sparkle icon (`Sparkles` from lucide-react) as AI marker | `14-CONTEXT.md` D-04; confirmed in `components/editor/ParentClassPicker.tsx`, `SimilarConceptsPanel.tsx` |
| Inline suggestion cards below sections (not modal) | `14-CONTEXT.md` D-01 |
| Three affordances: accept / reject / edit | `14-CONTEXT.md` D-02; REQUIREMENTS.md UX-04 |
| Confidence badge with green/amber/red color coding | `14-CONTEXT.md` D-03 |
| Next/Prev navigation (no separate flashcard mode) | `14-CONTEXT.md` D-08 |
| Classes / Properties tab bar | `14-CONTEXT.md` D-10 |
| Domain/Range for ObjectProperty (replaces Children) | `14-CONTEXT.md` D-11 |
| Pending count badge in editor header | `14-CONTEXT.md` D-12 |
| Skeleton placeholder rows during loading | `14-CONTEXT.md` D-14 |
| Inline error banner on failure (no modal) | `14-CONTEXT.md` D-15 |
| Primary color: sky (`primary-500` = #0ea5e9) | `app/globals.css` `@theme` |
| Font: Inter + JetBrains Mono | `app/layout.tsx` |
| Icon library: lucide-react | `package.json`, existing component imports |
| `animate-pulse` for skeleton | `components/editor/CrossReferencesPanel.tsx`, `EntityHistoryTab.tsx` |
| `amber-*` colors for AI-adjacent UI | `components/editor/LLMRoleBadge.tsx`, `AutoSaveAffordanceBar.tsx` |
| Scope toggle default: "This class" | Discretion area (narrowest scope, safest default) |
| Accepted card 200ms green flash | Discretion area (confirmed by `transition-colors` pattern in codebase) |
| Rejected card 150ms collapse | Discretion area |
| 3 skeleton cards per section | Discretion area (D-14 left exact count open) |
| Dashed border for suggestion cards | Discretion area (signals "pending/not yet committed" state) |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
