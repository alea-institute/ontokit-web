---
phase: 13-validation-guardrails-suggestion-generation
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - ../ontokit-api/tests/unit/test_entity_validation.py
  - ../ontokit-api/tests/unit/test_suggestion_generation.py
  - ../ontokit-api/tests/unit/test_context_assembler.py
  - ../ontokit-api/tests/unit/conftest.py
autonomous: true
requirements: [VALID-01, VALID-02, VALID-03, VALID-04, VALID-05, VALID-06, GEN-01, GEN-02, GEN-03, GEN-04, GEN-05, GEN-06, GEN-07, GEN-08, GEN-09]

must_haves:
  truths:
    - "pytest --co discovers all stub tests without import errors"
    - "Each VALID-* requirement has at least one named test stub"
    - "Each GEN-* requirement has at least one named test stub"
  artifacts:
    - path: "../ontokit-api/tests/unit/test_entity_validation.py"
      provides: "Test stubs for VALID-01 through VALID-06"
      contains: "def test_"
    - path: "../ontokit-api/tests/unit/test_suggestion_generation.py"
      provides: "Test stubs for GEN-01 through GEN-09"
      contains: "def test_"
    - path: "../ontokit-api/tests/unit/test_context_assembler.py"
      provides: "Test stubs for OntologyContextAssembler"
      contains: "def test_"
  key_links: []
---

<objective>
Create test stubs for all Phase 13 services before implementation begins, ensuring pytest discovery works and downstream plans can verify Nyquist compliance.

Purpose: Wave 0 stubs guarantee that all subsequent plans have test files ready for real test implementation, preventing the "missing test file" failure mode.
Output: Three test stub files with pytest-discoverable async test functions covering all 15 requirement IDs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-validation-guardrails-suggestion-generation/13-CONTEXT.md
@.planning/phases/13-validation-guardrails-suggestion-generation/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for ValidationService and SuggestionGenerationService</name>
  <files>
    ../ontokit-api/tests/unit/test_entity_validation.py,
    ../ontokit-api/tests/unit/test_suggestion_generation.py,
    ../ontokit-api/tests/unit/test_context_assembler.py,
    ../ontokit-api/tests/unit/conftest.py
  </files>
  <read_first>
    ../ontokit-api/tests/unit/test_duplicate_check.py
    ../ontokit-api/tests/unit/test_reasoner_validation.py
    ../ontokit-api/tests/unit/conftest.py
  </read_first>
  <action>
Create three test stub files following the established Phase 12 Wave 0 pattern (see test_duplicate_check.py for the async pytest pattern).

**File 1: tests/unit/test_entity_validation.py**
Create these stub tests (each `pytest.skip("Wave 0 stub — implementation in Plan 13-01")`):
- `test_valid01_parent_required_rejects_no_parent` — VALID-01: entity with empty parent_iris fails
- `test_valid01_parent_required_accepts_with_parent` — VALID-01: entity with parent_iris passes
- `test_valid02_english_label_rejects_missing` — VALID-02: entity with no English label fails
- `test_valid02_english_label_accepts_en` — VALID-02: entity with lang="en" label passes
- `test_valid03_cycle_detection_blocks_cycle` — VALID-03: proposed parent that creates cycle is blocked
- `test_valid03_cycle_detection_passes_no_cycle` — VALID-03: valid parent passes
- `test_valid04_namespace_rejects_foreign` — VALID-04: IRI in foreign namespace blocked
- `test_valid04_namespace_accepts_owned` — VALID-04: IRI in project namespace passes
- `test_valid05_returns_structured_errors` — VALID-05: validation errors are list[ValidationError] with field, code, message
- `test_valid06_iri_minting_uses_uuid` — VALID-06: minted IRI matches pattern `{namespace}{uuid4}`
- `test_valid06_iri_minting_detects_namespace` — VALID-06: auto-detect namespace from ontology

**File 2: tests/unit/test_suggestion_generation.py**
Create these stub tests:
- `test_gen01_generate_children` — GEN-01: suggestion_type="children" returns child class suggestions
- `test_gen02_generate_siblings` — GEN-02: suggestion_type="siblings" returns sibling suggestions
- `test_gen03_generate_annotations` — GEN-03: suggestion_type="annotations" returns annotation suggestions
- `test_gen04_generate_parents` — GEN-04: suggestion_type="parents" returns parent suggestions
- `test_gen05_generate_edges` — GEN-05: suggestion_type="edges" returns edge suggestions
- `test_gen06_context_included_in_prompt` — GEN-06: LLM messages contain class+parents+siblings context
- `test_gen07_prompt_templates_based_on_generative_folio` — GEN-07: prompt contains structured instructions (legal ontology, JSON output)
- `test_gen08_confidence_score_normalized` — GEN-08: confidence is float 0-1 or None
- `test_gen09_provenance_tagged` — GEN-09: each suggestion has provenance="llm-proposed"
- `test_batch_size_configurable` — D-05: batch_size 1-10 controls number of suggestions
- `test_auto_validate_in_pipeline` — D-09: pipeline runs validation+dedup per suggestion
- `test_json_parse_handles_markdown_fences` — Pitfall 3: markdown fences stripped from LLM output
- `test_confidence_normalization_scales` — Pitfall 4: values >1.0 divided by 100

**File 3: tests/unit/test_context_assembler.py**
Create these stub tests:
- `test_assemble_context_includes_current_class` — current class IRI+labels+annotations in context
- `test_assemble_context_includes_parents` — parent classes with labels in context
- `test_assemble_context_includes_siblings` — siblings of current class in context
- `test_assemble_context_includes_existing_children` — existing children listed
- `test_assemble_context_respects_max_siblings` — max_siblings caps sibling count
- `test_assemble_context_handles_root_class` — class with no parents returns empty parents list

**conftest.py additions (if not already present):**
Add shared fixtures for Phase 13 tests:
- `mock_llm_provider` — AsyncMock for LLMProvider.chat() returning `("json_string", 100, 50)`
- `mock_ontology_index` — AsyncMock for OntologyIndexService with get_class_detail, get_class_children, get_ancestor_path
- `mock_duplicate_check_service` — AsyncMock for DuplicateCheckService.check() returning a pass verdict

All tests must be `async def` using `@pytest.mark.asyncio`. Import pattern: `import pytest`.
  </action>
  <verify>
    <automated>cd /home/damienriehl/Coding\ Projects/ontokit-api && source .venv/bin/activate && python -m pytest tests/unit/test_entity_validation.py tests/unit/test_suggestion_generation.py tests/unit/test_context_assembler.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" ../ontokit-api/tests/unit/test_entity_validation.py` returns 11 or more
    - `grep -c "def test_" ../ontokit-api/tests/unit/test_suggestion_generation.py` returns 13 or more
    - `grep -c "def test_" ../ontokit-api/tests/unit/test_context_assembler.py` returns 6 or more
    - `pytest --co -q` discovers all tests without import errors (exit code 0)
    - `grep "pytest.skip" ../ontokit-api/tests/unit/test_entity_validation.py` matches every test function
    - `grep "mock_llm_provider\|mock_ontology_index\|mock_duplicate_check_service" ../ontokit-api/tests/unit/conftest.py` returns at least 3 matches
  </acceptance_criteria>
  <done>All 30+ test stubs discoverable by pytest, all skip with Wave 0 message, conftest has shared Phase 13 fixtures</done>
</task>

</tasks>

<verification>
cd /home/damienriehl/Coding\ Projects/ontokit-api && source .venv/bin/activate && python -m pytest tests/unit/ -q --tb=short
All existing tests still pass, new stubs are collected and skipped.
</verification>

<success_criteria>
- pytest discovers 30+ new test stubs across 3 files
- All stubs skip cleanly (no import errors, no failures)
- Existing test suite remains green
- conftest.py has reusable Phase 13 mock fixtures
</success_criteria>

<output>
After completion, create `.planning/phases/13-validation-guardrails-suggestion-generation/13-00-SUMMARY.md`
</output>
