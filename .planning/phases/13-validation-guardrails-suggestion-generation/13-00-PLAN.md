---
phase: 13-validation-guardrails-suggestion-generation
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - ../ontokit-api/tests/unit/test_validation_service.py
  - ../ontokit-api/tests/unit/test_suggestion_generation.py
  - ../ontokit-api/tests/unit/test_context_assembler.py
  - ../ontokit-api/tests/unit/test_prompt_templates.py
  - ../ontokit-api/tests/unit/test_iri_minting.py
autonomous: true
requirements: [VALID-01, VALID-02, VALID-03, VALID-04, VALID-05, VALID-06, GEN-01, GEN-02, GEN-03, GEN-04, GEN-05, GEN-06, GEN-07, GEN-08, GEN-09]

must_haves:
  truths:
    - "All downstream plans can run pytest --co without discovery failures"
    - "Every VALID-* and GEN-* requirement has at least one skip-marked test stub"
  artifacts:
    - path: "../ontokit-api/tests/unit/test_validation_service.py"
      provides: "Skip stubs for VALID-01 through VALID-06"
      min_lines: 40
    - path: "../ontokit-api/tests/unit/test_suggestion_generation.py"
      provides: "Skip stubs for GEN-01 through GEN-05, GEN-08, GEN-09"
      min_lines: 40
    - path: "../ontokit-api/tests/unit/test_context_assembler.py"
      provides: "Skip stubs for GEN-06 context assembly"
      min_lines: 20
    - path: "../ontokit-api/tests/unit/test_prompt_templates.py"
      provides: "Skip stubs for GEN-07 prompt templates"
      min_lines: 20
    - path: "../ontokit-api/tests/unit/test_iri_minting.py"
      provides: "Skip stubs for VALID-06 IRI minting"
      min_lines: 15
  key_links: []
---

<objective>
Wave 0: Create pytest skip stubs for all Phase 13 test files so downstream plans (01, 02, 03) can run `pytest --co` without discovery failures.

Purpose: Nyquist compliance — every plan must have test files discoverable before implementation begins.
Output: 5 test stub files with skip-marked tests covering all VALID-* and GEN-* requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-validation-guardrails-suggestion-generation/13-CONTEXT.md
@.planning/phases/13-validation-guardrails-suggestion-generation/13-RESEARCH.md

<!-- Existing test stubs pattern from Phase 12 -->
@../ontokit-api/tests/unit/test_duplicate_check.py (first 30 lines — skip stub pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all Wave 0 test stub files</name>
  <files>
    ../ontokit-api/tests/unit/test_validation_service.py
    ../ontokit-api/tests/unit/test_suggestion_generation.py
    ../ontokit-api/tests/unit/test_context_assembler.py
    ../ontokit-api/tests/unit/test_prompt_templates.py
    ../ontokit-api/tests/unit/test_iri_minting.py
  </files>
  <read_first>
    ../ontokit-api/tests/unit/test_duplicate_check.py (first 30 lines — see skip pattern)
    ../ontokit-api/tests/unit/test_structural_similarity.py (first 30 lines — see skip pattern)
  </read_first>
  <action>
Create 5 test files following the established Wave 0 pattern: `@pytest.mark.skip(reason="Wave 0 stub -- implementation in Plan XX")` with descriptive docstrings.

**File 1: `test_validation_service.py`** — 7 skip stubs for Plan 01:
- `test_parent_required` — VALID-01: entity with empty parent_iris returns ValidationError with code="VALID-01"
- `test_parent_required_passes` — VALID-01: entity with at least one parent_iri passes
- `test_english_label_required` — VALID-02: entity with no English label returns code="VALID-02"
- `test_english_label_passes` — VALID-02: entity with lang="en" label passes
- `test_cycle_detection` — VALID-03: assigning parent that is a descendant returns code="VALID-03"
- `test_namespace_check` — VALID-04: IRI in foreign namespace returns code="VALID-04"
- `test_error_messages_human_readable` — VALID-05: every ValidationError has a non-empty `message` field

**File 2: `test_suggestion_generation.py`** — 9 skip stubs for Plan 03:
- `test_generate_children` — GEN-01: type="children" returns list of GeneratedSuggestion
- `test_generate_siblings` — GEN-02: type="siblings" returns list of GeneratedSuggestion
- `test_generate_annotations` — GEN-03: type="annotations" returns list of GeneratedSuggestion
- `test_generate_parents` — GEN-04: type="parents" returns list of GeneratedSuggestion with cycle-check validation
- `test_generate_edges` — GEN-05: type="edges" returns list of GeneratedSuggestion
- `test_confidence_normalization` — GEN-08: confidence values normalized to 0-1 float or None
- `test_provenance_llm_proposed` — GEN-09: all generated suggestions have provenance="llm-proposed"
- `test_pipeline_auto_validates` — D-09: each suggestion in response includes validation_errors list
- `test_pipeline_auto_dedup` — D-09: each suggestion in response includes duplicate_verdict

**File 3: `test_context_assembler.py`** — 4 skip stubs for Plan 02:
- `test_assembles_current_class` — GEN-06: context dict includes current_class with iri, labels, annotations
- `test_assembles_parents` — GEN-06: context dict includes parents list with iri, label, annotations
- `test_assembles_siblings` — GEN-06: context dict includes siblings list (children of first parent, excluding current)
- `test_assembles_existing_children` — GEN-06: context dict includes existing_children list

**File 4: `test_prompt_templates.py`** — 6 skip stubs for Plan 02:
- `test_children_prompt_returns_messages` — GEN-07: children.build_messages() returns list of dicts with role/content keys
- `test_siblings_prompt_returns_messages` — GEN-07: siblings.build_messages() returns messages
- `test_annotations_prompt_returns_messages` — GEN-07: annotations.build_messages() returns messages
- `test_parents_prompt_returns_messages` — GEN-07: parents.build_messages() returns messages
- `test_edges_prompt_returns_messages` — GEN-07: edges.build_messages() returns messages
- `test_prompt_includes_batch_size` — GEN-07: batch_size parameter appears in user message content

**File 5: `test_iri_minting.py`** — 3 skip stubs for Plan 01:
- `test_mint_iri_uuid_format` — VALID-06: minted IRI contains UUID v4 pattern after namespace separator
- `test_mint_iri_uses_namespace` — VALID-06: minted IRI starts with provided namespace
- `test_detect_project_namespace` — D-12: auto-detect from project.ontology_iri or fallback

Each stub must:
1. Use `import pytest` at the top
2. Use `@pytest.mark.skip(reason="Wave 0 stub -- implementation in Plan {NN}")` decorator
3. Have a docstring referencing the requirement ID
4. Have `pass` as the body
5. Use `async def` for tests that will need async (service calls)
  </action>
  <verify>
    <automated>cd ../ontokit-api && source .venv/bin/activate && python -m pytest tests/unit/test_validation_service.py tests/unit/test_suggestion_generation.py tests/unit/test_context_assembler.py tests/unit/test_prompt_templates.py tests/unit/test_iri_minting.py --co -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_validation_service.py contains `def test_parent_required` and `def test_cycle_detection` and `def test_namespace_check`
    - tests/unit/test_suggestion_generation.py contains `def test_generate_children` and `def test_generate_edges` and `def test_provenance_llm_proposed`
    - tests/unit/test_context_assembler.py contains `def test_assembles_current_class` and `def test_assembles_parents`
    - tests/unit/test_prompt_templates.py contains `def test_children_prompt_returns_messages` and `def test_edges_prompt_returns_messages`
    - tests/unit/test_iri_minting.py contains `def test_mint_iri_uuid_format` and `def test_detect_project_namespace`
    - `pytest --co -q` on all 5 files collects 29 tests with 0 errors
  </acceptance_criteria>
  <done>All 29 skip-marked test stubs exist across 5 files and are discoverable by pytest without import errors</done>
</task>

</tasks>

<verification>
- `cd ../ontokit-api && source .venv/bin/activate && python -m pytest tests/unit/test_validation_service.py tests/unit/test_suggestion_generation.py tests/unit/test_context_assembler.py tests/unit/test_prompt_templates.py tests/unit/test_iri_minting.py --co -q` collects 29 tests
- `python -m pytest tests/unit/ -q --tb=short` passes (all 29 are skipped, no failures)
</verification>

<success_criteria>
29 skip-marked test stubs across 5 files, all discoverable by pytest, covering every VALID-* and GEN-* requirement ID. Downstream plans 01, 02, and 03 can run `pytest --co` without discovery failures.
</success_criteria>

<output>
After completion, create `.planning/phases/13-validation-guardrails-suggestion-generation/13-00-SUMMARY.md`
</output>
