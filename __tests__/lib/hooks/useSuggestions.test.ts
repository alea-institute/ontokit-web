import { describe, it } from "vitest";

describe("useSuggestions", () => {
  it.skip("request() calls generationApi.generateSuggestions and stores results", () => {
    // Wave 0 stub — implementation in Plan 02
  });

  it.skip("returns isLoading=true while API call is in flight", () => {
    // Wave 0 stub — implementation in Plan 02
  });

  it.skip("returns error string when API call fails", () => {
    // Wave 0 stub — implementation in Plan 02
  });

  it.skip("accept() calls onAccepted callback with suggestion and editedValue", () => {
    // Wave 0 stub — implementation in Plan 02
  });

  it.skip("reject() sets suggestion status to rejected in store", () => {
    // Wave 0 stub — implementation in Plan 02
  });
});
