import { describe, it } from "vitest";

describe("byoKeyStore", () => {
  it.skip("stores BYO key by projectId in localStorage", () => {
    // Wave 0 stub — implementation in Plan 03
  });

  it.skip("retrieves stored BYO key for a project", () => {
    // Wave 0 stub — implementation in Plan 03
  });

  it.skip("clears BYO key for a project", () => {
    // Wave 0 stub — implementation in Plan 03
  });

  it.skip("marks key as validated with timestamp", () => {
    // Wave 0 stub — implementation in Plan 03
  });
});
